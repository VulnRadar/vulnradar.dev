// Moved to components/shared/url-display.tsx so /badge renders a scan's URL
// exactly the way /compare does. Re-exported here because this path is what
// the compare components already import.
export { UrlDisplay } from "@/components/shared/url-display";
