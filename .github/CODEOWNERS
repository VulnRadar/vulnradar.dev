# ============================================================================
# VulnRadar Code Owners
# ============================================================================
# Auto-assigns reviewers based on which paths the PR touches.
# VulnRadar is currently a 2-person org (dbcaa, RejectModders); both are
# listed as owners for all paths until sub-teams are created.
# ============================================================================

# Default owner for everything not matched below
*       @dbcaa @RejectModders

# Security-critical paths
/lib/auth/          @dbcaa @RejectModders
/lib/scanner/       @dbcaa @RejectModders
/lib/rate-limiting/ @dbcaa @RejectModders
/lib/billing/       @dbcaa @RejectModders
# v3, not v2. app/api/ holds only security-txt/, v3/ and version/, so the
# three v2 rules these replace matched nothing: the catch-all above still
# assigned both owners, but the paths written down here to mark the
# security-critical API surface pointed at directories that do not exist.
/app/api/v3/auth/   @dbcaa @RejectModders
/app/api/v3/billing/ @dbcaa @RejectModders
/app/api/v3/webhooks/ @dbcaa @RejectModders

# Configuration changes
/lib/config/        @dbcaa @RejectModders
/.env.example       @dbcaa @RejectModders
/next.config.mjs    @dbcaa @RejectModders
/Dockerfile         @dbcaa @RejectModders
/docker-compose.yml @dbcaa @RejectModders

# CI / infrastructure
/.github/           @dbcaa @RejectModders

# Database migrations
# Both runners moved into their own directories; the old single-file paths
# had stopped matching anything.
/lib/database/      @dbcaa @RejectModders
/scripts/migrate/   @dbcaa @RejectModders
/scripts/create-fresh-db/ @dbcaa @RejectModders
