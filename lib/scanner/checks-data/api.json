[
  {
    "id": "api-rest-allow-methods-trace",
    "type": "body-pattern",
    "title": "TRACE method referenced in API response body",
    "category": "api",
    "severity": "low",
    "description": "A response body explicitly lists TRACE as a supported method (e.g. in a route config or API documentation payload the server exposes), which is unusual enough to be worth checking even though it doesn't confirm the verb is actually enabled.",
    "evidence": "Response body at {{url}} references TRACE as a supported method - confirm whether the TRACE verb is actually enabled on the server before treating this as exploitable.",
    "riskImpact": "Cross-Site Tracing (XST) lets an attacker reflect a victim's cookies and Authorization headers, including HttpOnly cookies, back through the browser, bypassing the HttpOnly restriction and enabling session theft, if TRACE is genuinely enabled.",
    "explanation": "This is a content match on the response body only, not a live TRACE request. See the separate 'HTTP TRACE method enabled' finding for the Allow-header signal, which is the more direct indicator that the verb is active.",
    "fixSteps": [
      "Disable TRACE on the server or reverse proxy",
      "In Nginx: return 405 for TRACE requests",
      "In Apache: add TraceEnable Off to httpd.conf",
      "In Express: reject TRACE at the middleware layer before routing"
    ],
    "codeExamples": [
      {
        "label": "Nginx: disable TRACE",
        "language": "nginx",
        "code": "# In your server block\nif ($request_method = TRACE) {\n    return 405;\n}"
      },
      {
        "label": "Apache: disable TRACE",
        "language": "apache",
        "code": "# In httpd.conf or .htaccess\nTraceEnable Off"
      },
      {
        "label": "Express.js: reject TRACE",
        "language": "javascript",
        "code": "app.use((req, res, next) => {\n  if (req.method === 'TRACE') {\n    return res.status(405).end();\n  }\n  next();\n});"
      }
    ],
    "references": [
      "https://owasp.org/www-community/attacks/Cross_Site_Tracing",
      "https://httpwg.org/specs/rfc9110.html#TRACE"
    ],
    "_idx": 0,
    "cwe": "CWE-16",
    "owasp": "A05:2021"
  },
  {
    "id": "api-rest-allow-methods-delete",
    "type": "header",
    "title": "REST endpoint allows DELETE",
    "category": "api",
    "severity": "info",
    "description": "REST endpoint exposes DELETE without authentication.",
    "evidence": "Server Allow header for {{url}} lists DELETE - authentication was not tested by this scan; verify separately that DELETE requires proper auth.",
    "riskImpact": "Unauthenticated DELETE endpoints allow any internet-reachable client to permanently destroy resources, enabling data-loss attacks or cascading failures in downstream systems that depend on those records.",
    "explanation": "REST endpoint exposes DELETE without authentication.",
    "fixSteps": [
      "Require authentication on all state-changing verbs (DELETE, PATCH, PUT, POST)",
      "Return 401 for missing tokens and 403 for insufficient scope",
      "Use middleware to enforce auth before any route handler runs"
    ],
    "codeExamples": [
      {
        "label": "Express.js: auth guard middleware",
        "language": "javascript",
        "code": "function requireAuth(req, res, next) {\n  const token = req.headers.authorization?.split(' ')[1];\n  if (!token) return res.status(401).json({ error: 'Unauthorized' });\n  try {\n    req.user = jwt.verify(token, process.env.JWT_SECRET);\n    next();\n  } catch {\n    res.status(401).json({ error: 'Invalid token' });\n  }\n}\n\n// Apply before DELETE route\nrouter.delete('/resource/:id', requireAuth, deleteHandler);"
      },
      {
        "label": "Next.js Route Handler: auth check",
        "language": "typescript",
        "code": "import { getSession } from '@/lib/auth';\nimport { NextResponse } from 'next/server';\n\nexport async function DELETE(req: Request) {\n  const session = await getSession();\n  if (!session) {\n    return NextResponse.json({ error: 'Unauthorized' }, { status: 401 });\n  }\n  // ... delete logic\n}"
      }
    ],
    "references": [
      "https://owasp.org/API-Security/editions/2023/en/0xa2-broken-authentication/",
      "https://cheatsheetseries.owasp.org/cheatsheets/REST_Security_Cheat_Sheet.html"
    ],
    "_idx": 1,
    "cwe": "CWE-284",
    "owasp": "A01:2021"
  },
  {
    "id": "api-graphql-suggestions-enabled",
    "type": "combined",
    "title": "GraphQL introspection field suggestions",
    "category": "api",
    "severity": "low",
    "description": "Field suggestions on error responses let attackers enumerate the schema.",
    "evidence": "GraphQL error response at {{url}} contains a 'Did you mean ...' suggestion, confirming field suggestions are enabled.",
    "riskImpact": "Field suggestions in GraphQL error messages let attackers enumerate valid field names one request at a time, reconstructing the schema even when introspection is disabled, exposing internal data models and admin-only fields.",
    "explanation": "Field suggestions on error responses let attackers enumerate the schema.",
    "fixSteps": [
      "Disable field suggestions using Apollo's formatError hook in production",
      "In Yoga, configure maskedErrors to replace suggestion text with a generic message",
      "Log full errors server-side only; return a generic message to clients"
    ],
    "codeExamples": [
      {
        "label": "Apollo Server 4: disable suggestions",
        "language": "javascript",
        "code": "import { ApolloServer } from '@apollo/server';\n\nconst server = new ApolloServer({\n  typeDefs,\n  resolvers,\n  formatError(formattedError) {\n    if (process.env.NODE_ENV === 'production') {\n      return { message: 'Internal server error' };\n    }\n    return formattedError;\n  },\n});"
      },
      {
        "label": "GraphQL Yoga: masked errors",
        "language": "javascript",
        "code": "import { createYoga, maskError } from 'graphql-yoga';\n\nconst yoga = createYoga({\n  schema,\n  maskedErrors: {\n    maskError(error, message) {\n      return maskError(error, 'Unexpected error');\n    },\n  },\n});"
      }
    ],
    "references": [
      "https://www.apollographql.com/docs/apollo-server/security/errors/",
      "https://owasp.org/API-Security/editions/2023/en/0xa8-security-misconfiguration/"
    ],
    "_idx": 2,
    "cwe": "CWE-200",
    "owasp": "A05:2021"
  },
  {
    "id": "api-graphql-no-rate-limit",
    "type": "header",
    "title": "GraphQL endpoint has no rate-limit headers",
    "category": "api",
    "severity": "medium",
    "description": "GraphQL POST endpoints need rate-limiting because one query can touch many resolvers.",
    "evidence": "GraphQL endpoint at {{url}} returned no rate-limit headers (X-RateLimit-*, RateLimit-*) after repeated requests.",
    "riskImpact": "A single GraphQL request can fan out to dozens of resolvers, consuming database connections and CPU far beyond what a per-request IP limit catches, letting attackers exhaust server resources with a small number of cheap HTTP calls.",
    "explanation": "GraphQL POST endpoints need rate-limiting because one query can touch many resolvers.",
    "fixSteps": [
      "Rate-limit by query cost and depth, not just request count",
      "Use graphql-rate-limit or a token-bucket middleware keyed on the authenticated user",
      "Return 429 with Retry-After when limits are exceeded"
    ],
    "codeExamples": [
      {
        "label": "graphql-rate-limit: per-user rate limit",
        "language": "javascript",
        "code": "import { createRateLimitDirective, defaultKeyGenerator } from 'graphql-rate-limit';\n\nconst rateLimitDirective = createRateLimitDirective({\n  keyGenerator: (directiveArgs, obj, args, context) =>\n    `${context.user?.id ?? context.ip}:${defaultKeyGenerator(directiveArgs, obj, args, context)}`,\n});\n\n// In SDL:\n// type Query {\n//   searchUsers(q: String!): [User] @rateLimit(window: \"1m\", max: 20)\n// }"
      },
      {
        "label": "Express: global rate limit for GraphQL endpoint",
        "language": "javascript",
        "code": "import rateLimit from 'express-rate-limit';\n\nconst gqlLimiter = rateLimit({\n  windowMs: 60 * 1000,\n  max: 60,\n  message: { errors: [{ message: 'Too many requests' }] },\n  standardHeaders: true,\n  legacyHeaders: false,\n});\n\napp.use('/graphql', gqlLimiter);"
      }
    ],
    "references": [
      "https://github.com/teamplanes/graphql-rate-limit",
      "https://owasp.org/API-Security/editions/2023/en/0xa4-unrestricted-resource-consumption/"
    ],
    "_idx": 3,
    "cwe": "CWE-799",
    "owasp": "A04:2021"
  },
  {
    "id": "api-openapi-server-url-leak",
    "type": "body-pattern",
    "title": "OpenAPI server URL leaks internal host",
    "category": "api",
    "severity": "low",
    "description": "swagger.json often contains the internal / staging host in the 'servers' array.",
    "evidence": "OpenAPI document at {{url}} contains a 'servers' entry with a private or staging hostname.",
    "riskImpact": "Internal hostnames in public OpenAPI documents reveal network topology, staging infrastructure, and internal DNS naming conventions, information that aids targeted reconnaissance and phishing attacks against internal services.",
    "explanation": "swagger.json often contains the internal / staging host in the 'servers' array.",
    "fixSteps": [
      "Generate environment-specific OpenAPI documents; exclude internal hosts from the public document",
      "Use relative server URLs (/ or /api) in the public-facing document",
      "Strip servers[] from public docs using a build-time transform or swagger-combine"
    ],
    "codeExamples": [
      {
        "label": "OpenAPI 3.0: relative server URL (safe)",
        "language": "yaml",
        "code": "openapi: '3.0.3'\ninfo:\n  title: My API\n  version: '1.0'\nservers:\n  - url: /api/v1\n    description: Current deployment"
      },
      {
        "label": "Node.js: strip internal servers at runtime",
        "language": "javascript",
        "code": "import swaggerDoc from './openapi.json';\n\nconst publicDoc = {\n  ...swaggerDoc,\n  servers: swaggerDoc.servers?.filter(\n    (s) => !s.url.includes('internal') && !s.url.includes('staging')\n  ) ?? [{ url: '/api/v1' }],\n};"
      }
    ],
    "references": [
      "https://owasp.org/API-Security/editions/2023/en/0xa8-security-misconfiguration/",
      "https://spec.openapis.org/oas/v3.1.0#server-object"
    ],
    "_idx": 4,
    "cwe": "CWE-200",
    "owasp": "A05:2021"
  },
  {
    "id": "api-cors-preflight-cache-missing",
    "type": "header",
    "title": "CORS preflight result not cached",
    "category": "api",
    "severity": "low",
    "description": "Missing Access-Control-Max-Age forces browsers to send preflight OPTIONS on every cross-origin request.",
    "evidence": "CORS preflight response at {{url}} is missing the Access-Control-Max-Age header.",
    "riskImpact": "Without preflight caching, every cross-origin API call from browser clients triggers an extra OPTIONS round-trip, doubling perceived latency for API consumers and increasing server load under heavy traffic.",
    "explanation": "Missing Access-Control-Max-Age forces browsers to send preflight OPTIONS on every cross-origin request.",
    "fixSteps": [
      "Add Access-Control-Max-Age: 600 (10 minutes) for stable CORS configs",
      "Do not set it above 86400 (24 hours); a stale policy can block legitimate requests after a config change"
    ],
    "codeExamples": [
      {
        "label": "Express cors(): set max age",
        "language": "javascript",
        "code": "import cors from 'cors';\n\napp.use(cors({\n  origin: ['https://app.example.com'],\n  methods: ['GET', 'POST', 'PUT', 'DELETE'],\n  allowedHeaders: ['Authorization', 'Content-Type'],\n  maxAge: 600,\n}));"
      },
      {
        "label": "Nginx: CORS preflight with max age",
        "language": "nginx",
        "code": "location /api/ {\n    if ($request_method = OPTIONS) {\n        add_header Access-Control-Allow-Origin  'https://app.example.com';\n        add_header Access-Control-Allow-Methods 'GET, POST, PUT, DELETE';\n        add_header Access-Control-Allow-Headers 'Authorization, Content-Type';\n        add_header Access-Control-Max-Age       600;\n        return 204;\n    }\n}"
      }
    ],
    "references": [
      "https://developer.mozilla.org/en-US/docs/Web/HTTP/Headers/Access-Control-Max-Age",
      "https://fetch.spec.whatwg.org/#http-cors-protocol"
    ],
    "_idx": 5
  },
  {
    "id": "api-bearer-header-leak",
    "type": "url-check",
    "title": "Bearer token in URL query string",
    "category": "api",
    "severity": "high",
    "description": "Authorization: Bearer is fine, but a Bearer token in a URL query string can leak via logs.",
    "evidence": "A JWT- or opaque-token-shaped value was detected in the URL query string on {{url}}.",
    "riskImpact": "Tokens in URLs appear in server access logs, browser history, Referer headers, and CDN logs, any of which may be accessible to third parties. A single log export or shoulder-surf leaks all active sessions.",
    "explanation": "Authorization: Bearer is fine, but a Bearer token in a URL query string can leak via logs. Matching only the parameter name would also flag ordinary one-time links (password reset, email verification), so this requires the value itself to look like a JWT or a long opaque token, and excludes common one-time-link paths.",
    "fixSteps": [
      "Send Bearer tokens only in the Authorization header",
      "Never accept tokens as query parameters (?token=...) in production endpoints",
      "If cookies are used, set Secure; HttpOnly; SameSite=Strict and add CSRF protection"
    ],
    "codeExamples": [
      {
        "label": "Correct: Authorization header",
        "language": "javascript",
        "code": "const response = await fetch('/api/resource', {\n  headers: {\n    Authorization: `Bearer ${accessToken}`,\n    'Content-Type': 'application/json',\n  },\n});"
      },
      {
        "label": "Server: reject token in query string",
        "language": "javascript",
        "code": "app.use((req, res, next) => {\n  if (req.query.token || req.query.access_token) {\n    return res.status(400).json({\n      error: 'Tokens must be sent in the Authorization header, not the URL.',\n    });\n  }\n  next();\n});"
      }
    ],
    "references": [
      "https://datatracker.ietf.org/doc/html/rfc6750#section-2.3",
      "https://owasp.org/API-Security/editions/2023/en/0xa2-broken-authentication/"
    ],
    "_idx": 6,
    "cwe": "CWE-598",
    "owasp": "A02:2021"
  },
  {
    "id": "api-jsonp-callback",
    "type": "combined",
    "title": "JSONP callback parameter accepted",
    "category": "api",
    "severity": "medium",
    "description": "JSONP allows arbitrary JS to be loaded in the victim's origin. Prefer CORS.",
    "evidence": "API endpoint at {{url}} wraps its response in a callback function name supplied via a query parameter.",
    "riskImpact": "JSONP endpoints can be embedded as <script src> from any origin, letting attackers steal response data by overriding the callback. They also bypass Content Security Policy and cannot be protected by SameSite cookies.",
    "explanation": "JSONP allows arbitrary JS to be loaded in the victim's origin. Prefer CORS.",
    "fixSteps": [
      "Disable JSONP support and serve standard JSON with CORS headers instead",
      "If legacy clients require JSONP, validate the callback name against an allowlist of safe identifiers",
      "Never allow arbitrary JavaScript identifiers as the callback value"
    ],
    "codeExamples": [
      {
        "label": "Migrate from JSONP to CORS (Express)",
        "language": "javascript",
        "code": "import cors from 'cors';\n\n// Before (JSONP: insecure):\n// app.get('/data', (req, res) => {\n//   const cb = req.query.callback;\n//   res.send(`${cb}(${JSON.stringify(data)})`);\n// });\n\n// After (CORS: secure):\napp.use(cors({ origin: 'https://yourapp.com' }));\napp.get('/data', (req, res) => {\n  res.json(data);\n});"
      }
    ],
    "references": [
      "https://portswigger.net/web-security/cors/same-origin-policy",
      "https://cheatsheetseries.owasp.org/cheatsheets/REST_Security_Cheat_Sheet.html"
    ],
    "_idx": 7,
    "cwe": "CWE-352",
    "owasp": "A01:2021"
  },
  {
    "id": "api-rest-allow-methods-put-no-auth",
    "type": "header",
    "title": "PUT method listed in Allow header",
    "category": "api",
    "severity": "info",
    "description": "PUT changes server state. This is a passive observation that the endpoint's Allow header advertises PUT support (normal for any REST API with a PUT route, including properly authenticated ones); it does not confirm the endpoint accepts unauthenticated writes.",
    "evidence": "Allow header for {{url}} lists PUT: {{evidence}}. Authentication was not tested by this scan; verify separately that PUT requires proper auth.",
    "riskImpact": "If PUT is genuinely unauthenticated, attackers can overwrite arbitrary resources: user profiles, configuration files, permissions, without any proof of identity. A single request can corrupt data for all users.",
    "explanation": "An Allow header listing PUT only confirms the verb is routed on this path, which is expected for any REST API with a PUT endpoint. It says nothing about whether that endpoint enforces authentication; a properly protected PUT route still advertises PUT in Allow for CORS purposes.",
    "fixSteps": [
      "Require authentication on PUT and reject anonymous writes at the proxy or framework layer",
      "Validate that the authenticated user owns the resource being updated",
      "Use an allowlist of updatable fields to prevent mass assignment"
    ],
    "codeExamples": [
      {
        "label": "Express: authenticated PUT with ownership check",
        "language": "javascript",
        "code": "router.put('/users/:id', requireAuth, async (req, res) => {\n  if (req.user.id !== req.params.id && !req.user.isAdmin) {\n    return res.status(403).json({ error: 'Forbidden' });\n  }\n\n  const { name, email, bio } = req.body;\n  await db.users.update(req.params.id, { name, email, bio });\n  res.json({ success: true });\n});"
      }
    ],
    "references": [
      "https://owasp.org/API-Security/editions/2023/en/0xa3-broken-object-property-level-authorization/",
      "https://cheatsheetseries.owasp.org/cheatsheets/REST_Security_Cheat_Sheet.html"
    ],
    "_idx": 8,
    "cwe": "CWE-862",
    "owasp": "A01:2021"
  },
  {
    "id": "api-rest-allow-methods-patch-no-auth",
    "type": "header",
    "title": "PATCH method listed in Allow header",
    "category": "api",
    "severity": "info",
    "description": "PATCH partially mutates a resource. This is a passive observation that the endpoint's Allow header advertises PATCH support (normal for any REST API with a PATCH route, including properly authenticated ones); it does not confirm the endpoint accepts unauthenticated writes.",
    "evidence": "Allow header for {{url}} lists PATCH: {{evidence}}. Authentication was not tested by this scan; verify separately that PATCH requires proper auth.",
    "riskImpact": "If PATCH is genuinely unauthenticated, attackers can flip boolean fields (isAdmin, isActive, isVerified), change ownership IDs, or modify billing state: privilege escalation with no brute-force required.",
    "explanation": "An Allow header listing PATCH only confirms the verb is routed on this path, which is expected for any REST API with a PATCH endpoint. It says nothing about whether that endpoint enforces authentication; a properly protected PATCH route still advertises PATCH in Allow for CORS purposes.",
    "fixSteps": [
      "Require authentication on PATCH at the middleware layer before any routing",
      "Validate the JSON merge patch against an explicit allowlist of mutable fields",
      "Reject patches to privilege fields (role, isAdmin, isVerified) for non-admin callers"
    ],
    "codeExamples": [
      {
        "label": "Express: safe PATCH with field allowlist",
        "language": "javascript",
        "code": "const PATCHABLE_FIELDS = new Set(['name', 'bio', 'avatarUrl']);\n\nrouter.patch('/users/:id', requireAuth, async (req, res) => {\n  if (req.user.id !== req.params.id) {\n    return res.status(403).json({ error: 'Forbidden' });\n  }\n\n  const patch = Object.fromEntries(\n    Object.entries(req.body).filter(([k]) => PATCHABLE_FIELDS.has(k))\n  );\n\n  if (Object.keys(patch).length === 0) {\n    return res.status(400).json({ error: 'No valid fields to update.' });\n  }\n\n  await db.users.patch(req.params.id, patch);\n  res.json({ success: true });\n});"
      }
    ],
    "references": [
      "https://owasp.org/API-Security/editions/2023/en/0xa3-broken-object-property-level-authorization/",
      "https://datatracker.ietf.org/doc/html/rfc7396"
    ],
    "_idx": 9,
    "cwe": "CWE-862",
    "owasp": "A01:2021"
  },
  {
    "id": "api-rest-allow-methods-options-exposed",
    "type": "header",
    "title": "OPTIONS response exposes full method allowlist",
    "category": "api",
    "severity": "low",
    "description": "A verbose OPTIONS response (Allow: GET, POST, PUT, PATCH, DELETE) leaks the entire verb surface to attackers, simplifying endpoint enumeration.",
    "evidence": "OPTIONS request to {{url}} returned an Allow header listing methods beyond what the endpoint requires.",
    "riskImpact": "A verbose Allow header tells attackers exactly which HTTP methods are active, reducing the effort needed to map the API and craft targeted attacks against PUT, PATCH, or DELETE handlers.",
    "explanation": "A verbose OPTIONS response (Allow: GET, POST, PUT, PATCH, DELETE) leaks the entire verb surface to attackers, simplifying endpoint enumeration.",
    "fixSteps": [
      "Restrict Allow to only the verbs you actually serve on that specific path",
      "Disable unused HTTP methods at the web server or reverse proxy level",
      "Return 405 for any method not explicitly handled"
    ],
    "codeExamples": [
      {
        "label": "Express: explicit method list per route",
        "language": "javascript",
        "code": "router.route('/items')\n  .get(listItems)\n  .post(createItem);\n\n// Catch unsupported methods\nrouter.all('/items', (req, res) => {\n  res.set('Allow', 'GET, POST').status(405).json({ error: 'Method not allowed' });\n});"
      },
      {
        "label": "Nginx: restrict methods",
        "language": "nginx",
        "code": "location /api/items {\n    limit_except GET POST OPTIONS {\n        deny all;\n    }\n}"
      }
    ],
    "references": [
      "https://httpwg.org/specs/rfc9110.html#OPTIONS",
      "https://cheatsheetseries.owasp.org/cheatsheets/REST_Security_Cheat_Sheet.html"
    ],
    "_idx": 10,
    "cwe": "CWE-200",
    "owasp": "A05:2021"
  },
  {
    "id": "api-graphql-introspection-enabled",
    "type": "combined",
    "title": "GraphQL introspection enabled in production",
    "category": "api",
    "severity": "medium",
    "description": "__schema / __type queries dump the entire schema, including internal fields, admin-only mutations, and deprecation hints that aid targeted attacks.",
    "evidence": "Introspection keywords (__schema / __type / IntrospectionQuery) referenced by the /graphql endpoint at {{url}}. This does not confirm a schema dump was returned: send an introspection query to verify the server actually resolves it.",
    "riskImpact": "Introspection in production gives attackers a complete blueprint of your data model: every type, field, mutation, and subscription, eliminating the recon phase and exposing internal-only operations that untrusted clients should never call.",
    "explanation": "__schema / __type queries dump the entire schema, including internal fields, admin-only mutations, and deprecation hints that aid targeted attacks.",
    "fixSteps": [
      "Disable introspection in production (Apollo: introspection: false, GraphQL.NET: ExposeMetadata = false)",
      "Allow introspection only for authenticated admin users if needed for internal tooling",
      "Use persisted queries to give clients what they need without exposing the full schema"
    ],
    "codeExamples": [
      {
        "label": "Apollo Server 4: disable introspection in production",
        "language": "javascript",
        "code": "import { ApolloServer } from '@apollo/server';\n\nconst server = new ApolloServer({\n  typeDefs,\n  resolvers,\n  introspection: process.env.NODE_ENV !== 'production',\n});"
      },
      {
        "label": "GraphQL Yoga: NoSchemaIntrospectionCustomRule",
        "language": "javascript",
        "code": "import { createYoga } from 'graphql-yoga';\nimport { NoSchemaIntrospectionCustomRule } from 'graphql';\n\nconst yoga = createYoga({\n  schema,\n  plugins: [\n    {\n      onValidate({ addValidationRule }) {\n        if (process.env.NODE_ENV === 'production') {\n          addValidationRule(NoSchemaIntrospectionCustomRule);\n        }\n      },\n    },\n  ],\n});"
      }
    ],
    "references": [
      "https://graphql.org/learn/introspection/",
      "https://owasp.org/API-Security/editions/2023/en/0xa8-security-misconfiguration/"
    ],
    "_idx": 11,
    "cwe": "CWE-200",
    "owasp": "A05:2021"
  },
  {
    "id": "api-graphql-batch-queries",
    "type": "combined",
    "title": "GraphQL batch (array) queries accepted",
    "category": "api",
    "severity": "medium",
    "description": "Accepting an array of queries lets a single HTTP request fan out to N independent operations, multiplying rate-limit cost and bypassing per-request throttles.",
    "evidence": "GraphQL endpoint at {{url}} accepted an array of query objects in a single POST body.",
    "riskImpact": "A single batched HTTP request containing 100 queries executes 100 independent resolver trees while consuming only 1 unit of any per-request rate limit: a cheap amplification attack that can exhaust database connections and CPU without triggering throttling.",
    "explanation": "Accepting an array of queries lets a single HTTP request fan out to N independent operations, multiplying rate-limit cost and bypassing per-request throttles.",
    "fixSteps": [
      "Disable batching if not required by clients",
      "If batching is needed, cap the array size (e.g., max 10 items) and charge each item against the rate-limit budget",
      "In Apollo Server: set allowBatchedHttpRequests: false in the HTTP handler options"
    ],
    "codeExamples": [
      {
        "label": "Apollo Server: disable batching",
        "language": "javascript",
        "code": "import { expressMiddleware } from '@apollo/server/express4';\n\napp.use('/graphql', expressMiddleware(server, {\n  allowBatchedHttpRequests: false,\n}));"
      },
      {
        "label": "Custom batch size limit middleware",
        "language": "javascript",
        "code": "app.use('/graphql', (req, res, next) => {\n  if (Array.isArray(req.body) && req.body.length > 10) {\n    return res.status(400).json({\n      errors: [{ message: 'Batch size limit exceeded (max 10).' }],\n    });\n  }\n  next();\n});"
      }
    ],
    "references": [
      "https://www.apollographql.com/blog/batching-client-graphql-queries/",
      "https://owasp.org/API-Security/editions/2023/en/0xa4-unrestricted-resource-consumption/"
    ],
    "_idx": 12,
    "cwe": "CWE-799",
    "owasp": "A04:2021"
  },
  {
    "id": "api-graphql-error-stack-trace",
    "type": "combined",
    "title": "GraphQL error response leaks stack trace",
    "category": "api",
    "severity": "medium",
    "description": "Stack traces in error.extensions.stacktrace expose file paths, framework versions, and SQL fragments to any unauthenticated caller.",
    "evidence": "GraphQL error response at {{url}} contains a stacktrace field in error.extensions with internal file paths or SQL.",
    "riskImpact": "Stack traces embedded in API responses reveal internal file system paths, framework versions, database schema details, and SQL fragments, giving attackers the reconnaissance needed to craft targeted injection or path traversal attacks.",
    "explanation": "Stack traces in error.extensions.stacktrace expose file paths, framework versions, and SQL fragments to any unauthenticated caller.",
    "fixSteps": [
      "Use Apollo's formatError hook to strip stack traces in production",
      "Log full errors server-side and return only a generic message to clients",
      "Gate stack traces on NODE_ENV: development only"
    ],
    "codeExamples": [
      {
        "label": "Apollo Server: strip stack traces in production",
        "language": "javascript",
        "code": "const server = new ApolloServer({\n  typeDefs,\n  resolvers,\n  formatError(formattedError, error) {\n    console.error(error);\n\n    if (process.env.NODE_ENV === 'production') {\n      return {\n        message: formattedError.message,\n        extensions: { code: formattedError.extensions?.code },\n      };\n    }\n    return formattedError;\n  },\n});"
      }
    ],
    "references": [
      "https://www.apollographql.com/docs/apollo-server/security/errors/",
      "https://owasp.org/API-Security/editions/2023/en/0xa8-security-misconfiguration/"
    ],
    "_idx": 13,
    "cwe": "CWE-209",
    "owasp": "A05:2021"
  },
  {
    "id": "api-openapi-security-scheme-weak",
    "type": "combined",
    "title": "OpenAPI security scheme is weak or missing",
    "category": "api",
    "severity": "high",
    "description": "An OpenAPI document with no securitySchemes, or one that lists only apiKey in header, signals that the API does not enforce real authentication.",
    "evidence": "OpenAPI document at {{url}} has no securitySchemes defined or declares only an insecure API key scheme.",
    "riskImpact": "Missing or weak security schemes mean operations are undocumented from an access-control perspective. SDK generators produce unauthenticated clients, and automated scanners treat every endpoint as public.",
    "explanation": "An OpenAPI document with no securitySchemes, or one that lists only apiKey in header, signals that the API does not enforce real authentication.",
    "fixSteps": [
      "Declare a strong securityScheme (OAuth2 + JWT or mTLS) in the OpenAPI document",
      "Apply the scheme globally and override per-operation only for truly public endpoints",
      "Use spectral in CI to validate security scheme coverage"
    ],
    "codeExamples": [
      {
        "label": "OpenAPI 3.0: JWT bearer scheme applied globally",
        "language": "yaml",
        "code": "components:\n  securitySchemes:\n    bearerAuth:\n      type: http\n      scheme: bearer\n      bearerFormat: JWT\n\n# Apply globally: all operations require auth by default\nsecurity:\n  - bearerAuth: []\n\npaths:\n  /users:\n    get:\n      summary: List users\n      # Inherits global bearerAuth requirement\n      responses:\n        '200':\n          description: OK\n  /health:\n    get:\n      summary: Health check\n      security: []  # Override: public endpoint\n      responses:\n        '200':\n          description: OK"
      }
    ],
    "references": [
      "https://swagger.io/docs/specification/authentication/",
      "https://owasp.org/API-Security/editions/2023/en/0xa2-broken-authentication/"
    ],
    "_idx": 14,
    "cwe": "CWE-287",
    "owasp": "A07:2021"
  },
  {
    "id": "api-openapi-default-values-sensitive",
    "type": "combined",
    "title": "OpenAPI schema declares defaults for sensitive fields",
    "category": "api",
    "severity": "medium",
    "description": "Defaults such as isAdmin: false, role: user, or apiKey: REPLACE_ME teach attackers the shape of state and embed credentials in client SDKs.",
    "evidence": "OpenAPI schema at {{url}} declares a default value for a sensitive field such as role, isAdmin, or apiKey.",
    "riskImpact": "Default values for privilege fields in a public OpenAPI document tell attackers the exact property names and expected values used for access control, enabling targeted mass-assignment attacks to escalate privileges with a single crafted HTTP request.",
    "explanation": "Defaults such as isAdmin: false, role: user, or apiKey: REPLACE_ME teach attackers the shape of state and embed credentials in client SDKs.",
    "fixSteps": [
      "Remove default values from any field whose name contains token, password, key, role, admin, or secret",
      "Use writeOnly: true to mark privilege fields so SDKs exclude them from generated models",
      "Run spectral in CI to enforce no-defaults on sensitive properties"
    ],
    "codeExamples": [
      {
        "label": "OpenAPI: remove defaults from privilege fields",
        "language": "yaml",
        "code": "# Bad: exposes privilege structure\n# UserCreate:\n#   properties:\n#     role:\n#       type: string\n#       default: user      # remove this\n#     isAdmin:\n#       type: boolean\n#       default: false     # remove this\n\n# Good: no defaults on privilege fields\nUserCreate:\n  required:\n    - email\n    - password\n  properties:\n    email:\n      type: string\n      format: email\n    password:\n      type: string\n      format: password\n      writeOnly: true"
      }
    ],
    "references": [
      "https://owasp.org/API-Security/editions/2023/en/0xa3-broken-object-property-level-authorization/",
      "https://cheatsheetseries.owasp.org/cheatsheets/Mass_Assignment_Cheat_Sheet.html"
    ],
    "_idx": 15
  },
  {
    "id": "api-jwt-hs256-weak-secret",
    "type": "body-pattern",
    "title": "JWT HS256 signed with weak or hard-coded secret",
    "category": "api",
    "severity": "critical",
    "description": "HS256 trusts a shared secret. If the secret is short, default, or checked into source, attackers can sign forged tokens offline.",
    "evidence": "JWT at {{url}} is signed with HS256 and the secret appears to be weak, default, or hard-coded.",
    "riskImpact": "A weak HS256 secret (under 256 bits, or a known default like 'secret') can be cracked offline using hashcat in minutes. Once the secret is known, attackers can forge any JWT: impersonating any user, extending expiry, or granting admin access indefinitely.",
    "explanation": "HS256 trusts a shared secret. If the secret is short, default, or checked into source, attackers can sign forged tokens offline.",
    "fixSteps": [
      "Use at least 256 bits (32 bytes) of random entropy for HS256 secrets",
      "Prefer asymmetric algorithms (RS256, ES256) so the signing key never leaves the server",
      "Rotate secrets and invalidate all existing tokens if a weak or exposed secret is discovered"
    ],
    "codeExamples": [
      {
        "label": "Generate a strong HS256 secret",
        "language": "bash",
        "code": "# Generate a 256-bit (32 byte) random secret\nopenssl rand -base64 32\n\n# Store as an environment variable: never hard-code it\nexport JWT_SECRET=\"$(openssl rand -base64 32)\""
      },
      {
        "label": "Switch to RS256 (recommended)",
        "language": "javascript",
        "code": "import { SignJWT, jwtVerify, importPKCS8, importSPKI } from 'jose';\n\n// Sign with private key: only the server has this\nconst privateKey = await importPKCS8(process.env.JWT_PRIVATE_KEY, 'RS256');\nconst token = await new SignJWT({ sub: userId, role: 'user' })\n  .setProtectedHeader({ alg: 'RS256' })\n  .setExpirationTime('1h')\n  .setIssuedAt()\n  .sign(privateKey);\n\n// Verify with public key: safe to distribute\nconst publicKey = await importSPKI(process.env.JWT_PUBLIC_KEY, 'RS256');\nconst { payload } = await jwtVerify(token, publicKey, { algorithms: ['RS256'] });"
      }
    ],
    "references": [
      "https://auth0.com/blog/critical-vulnerabilities-in-json-web-token-libraries/",
      "https://datatracker.ietf.org/doc/html/rfc8725"
    ],
    "_idx": 16,
    "cwe": "CWE-326",
    "owasp": "A02:2021"
  },
  {
    "id": "api-cors-preflight-cache-over-24h",
    "type": "header",
    "title": "CORS preflight cache exceeds 24 hours",
    "category": "api",
    "severity": "low",
    "description": "Access-Control-Max-Age above 86400 pins browsers to a stale allowlist. New disallowed headers or origins take up to Max-Age seconds to take effect.",
    "evidence": "CORS preflight response at {{url}} has Access-Control-Max-Age set above 86400 seconds (24 hours).",
    "riskImpact": "An excessively long preflight cache means CORS policy changes, such as revoking an origin's access or removing a method, take days to propagate to browsers. Previously approved origins continue to receive cached preflight approval even after the server policy changes.",
    "explanation": "Access-Control-Max-Age above 86400 pins browsers to a stale allowlist. New disallowed headers or origins take up to Max-Age seconds to take effect.",
    "fixSteps": [
      "Cap Max-Age at 600 (10 minutes) for most APIs",
      "Use shorter windows (60s) during active CORS policy rollouts",
      "Most browsers cap Max-Age at 86400 (Chrome) or 600 (Firefox) regardless of what you set"
    ],
    "codeExamples": [
      {
        "label": "Express cors(): safe max age",
        "language": "javascript",
        "code": "import cors from 'cors';\n\napp.use(cors({\n  origin: ['https://app.example.com'],\n  maxAge: 600,\n}));"
      }
    ],
    "references": [
      "https://developer.mozilla.org/en-US/docs/Web/HTTP/Headers/Access-Control-Max-Age",
      "https://fetch.spec.whatwg.org/#http-cors-protocol"
    ],
    "_idx": 17
  },
  {
    "id": "api-rate-limit-per-ip-no-auth",
    "type": "header",
    "title": "Rate-limit keyed only on client IP, no auth required",
    "category": "api",
    "severity": "medium",
    "description": "Per-IP throttling is fine for anonymous traffic, but auth-less endpoints behind a shared NAT or proxy can be exhausted by one noisy neighbour.",
    "evidence": "API endpoint at {{url}} enforces rate limits only by IP with no authentication requirement.",
    "riskImpact": "IP-based rate limiting can be exhausted by a single user behind a corporate NAT or Tor exit node, blocking all users sharing that IP. Conversely, an attacker rotating IPs or using botnets can bypass it entirely for brute-force or scraping attacks.",
    "explanation": "Per-IP throttling is fine for anonymous traffic, but auth-less endpoints behind a shared NAT or proxy can be exhausted by one noisy neighbour.",
    "fixSteps": [
      "Require authentication on sensitive endpoints and key rate limits on the authenticated principal (user ID)",
      "Layer rate limits: per-IP for anonymous endpoints, per-user for authenticated endpoints",
      "Use Redis for distributed rate limiting across multiple server instances"
    ],
    "codeExamples": [
      {
        "label": "express-rate-limit: per-user after auth",
        "language": "javascript",
        "code": "import rateLimit from 'express-rate-limit';\n\nconst authRateLimit = rateLimit({\n  windowMs: 60 * 1000,\n  max: 100,\n  keyGenerator: (req) => req.user?.id ?? req.ip,\n  standardHeaders: true,\n  legacyHeaders: false,\n});\n\n// Apply after auth middleware so req.user is available\nrouter.use(requireAuth, authRateLimit);"
      }
    ],
    "references": [
      "https://owasp.org/API-Security/editions/2023/en/0xa4-unrestricted-resource-consumption/",
      "https://cheatsheetseries.owasp.org/cheatsheets/Denial_of_Service_Cheat_Sheet.html"
    ],
    "_idx": 18,
    "cwe": "CWE-799",
    "owasp": "A04:2021"
  },
  {
    "id": "api-rate-limit-headers-not-enforced-on-paths",
    "type": "header",
    "title": "Rate-limit headers present but only enforced on some paths",
    "category": "api",
    "severity": "medium",
    "description": "Returning X-RateLimit-* headers without enforcing the cap is theatre. Callers see budgets they cannot exceed in the response but can hammer the server anyway.",
    "evidence": "API at {{url}} returns rate-limit headers but continued accepting requests after the stated limit was reached.",
    "riskImpact": "Advertising rate limits that are not enforced creates a false sense of security. Automated scanners and attackers will ignore the headers and keep sending requests, while the server has no actual protection against abuse or DoS.",
    "explanation": "Returning X-RateLimit-* headers without enforcing the cap is theatre. Callers see budgets they cannot exceed in the response but can hammer the server anyway.",
    "fixSteps": [
      "Ensure every endpoint that emits X-RateLimit-* headers actually enforces the limit",
      "Test rate limiting in CI by sending requests above the stated limit and verifying 429 responses",
      "Apply rate limit middleware globally before route handlers rather than per-route to avoid gaps"
    ],
    "codeExamples": [
      {
        "label": "Express: global rate limit applied before all routes",
        "language": "javascript",
        "code": "import rateLimit from 'express-rate-limit';\n\n// Apply globally so no route can slip through\napp.use(rateLimit({\n  windowMs: 60 * 1000,\n  max: 100,\n  standardHeaders: true,\n  legacyHeaders: false,\n}));\n\n// Routes come after: rate limit is always applied\napp.use('/api', apiRouter);"
      }
    ],
    "references": [
      "https://owasp.org/API-Security/editions/2023/en/0xa4-unrestricted-resource-consumption/",
      "https://datatracker.ietf.org/doc/html/draft-ietf-httpapi-ratelimit-headers"
    ],
    "_idx": 19
  },
  {
    "id": "api-soap-soapaction-injection",
    "type": "header",
    "title": "SOAPAction header injection / SSRF",
    "category": "api",
    "severity": "high",
    "description": "The SOAPAction header value contains shell/URL metacharacters. If that value is passed verbatim to a downstream HTTP call without validation, attackers can pivot the request to internal services.",
    "evidence": "SOAPAction header observed on {{url}} contains metacharacters ({{evidence}}) that would be unsafe to concatenate into a downstream call without an allowlist.",
    "riskImpact": "An unvalidated SOAPAction header passed to outbound HTTP calls can redirect requests to internal services, cloud metadata endpoints (169.254.169.254), or other hosts the server can reach but the attacker cannot: a Server-Side Request Forgery (SSRF) attack. This finding observes the header value only; it does not confirm the server actually forwards it downstream.",
    "explanation": "This check inspects the SOAPAction header value for characters that would be dangerous if concatenated into a downstream URL. It does not observe whether the server actually forwards that value anywhere.",
    "fixSteps": [
      "Strict-allowlist the SOAPAction value against known operation names",
      "Never concatenate SOAPAction or any user-supplied string into outbound URLs",
      "Use a pre-built SOAP client library that validates operations against the WSDL"
    ],
    "codeExamples": [
      {
        "label": "Node.js: allowlist SOAPAction values",
        "language": "javascript",
        "code": "const ALLOWED_SOAP_ACTIONS = new Set([\n  'http://example.com/GetUserInfo',\n  'http://example.com/UpdateProfile',\n]);\n\napp.post('/soap', (req, res) => {\n  const action = req.headers['soapaction']?.replace(/\"/g, '');\n\n  if (!action || !ALLOWED_SOAP_ACTIONS.has(action)) {\n    return res.status(400).send(\n      '<soap:Fault><faultcode>Client</faultcode>' +\n      '<faultstring>Unknown SOAPAction</faultstring></soap:Fault>'\n    );\n  }\n  // Process the allowed action\n});"
      }
    ],
    "references": [
      "https://owasp.org/www-community/attacks/Server_Side_Request_Forgery",
      "https://cheatsheetseries.owasp.org/cheatsheets/Server_Side_Request_Forgery_Prevention_Cheat_Sheet.html"
    ],
    "_idx": 20,
    "cwe": "CWE-918",
    "owasp": "A10:2021"
  },
  {
    "id": "api-soap-xxe-enabled",
    "type": "body-pattern",
    "title": "SOAP/XML parser has external entities enabled (XXE)",
    "category": "api",
    "severity": "critical",
    "description": "XML parsers with DOCTYPE / external entity processing enabled allow attackers to read local files, hit SSRF endpoints, and exfiltrate via DTD.",
    "evidence": "A DOCTYPE declaration with an external ENTITY (SYSTEM or PUBLIC) reference was found in the response content served at {{url}}.",
    "riskImpact": "XML External Entity (XXE) attacks can read arbitrary files from the server filesystem (/etc/passwd, .env files, application configs), trigger SSRF against internal networks, and in some parsers enable remote code execution via Java serialization gadgets.",
    "explanation": "XML parsers with DOCTYPE / external entity processing enabled allow attackers to read local files, hit SSRF endpoints, and exfiltrate via DTD.",
    "fixSteps": [
      "Disable DOCTYPE declarations and external entity processing in every XML parser",
      "Use a hardened DocumentBuilderFactory / SAXParserFactory with all external features off",
      "In Node.js, use fast-xml-parser with processEntities: false"
    ],
    "codeExamples": [
      {
        "label": "Java: hardened JAXP parser (no XXE)",
        "language": "java",
        "code": "DocumentBuilderFactory dbf = DocumentBuilderFactory.newInstance();\ndbf.setFeature(\"http://apache.org/xml/features/disallow-doctype-decl\", true);\ndbf.setFeature(\"http://xml.org/sax/features/external-general-entities\", false);\ndbf.setFeature(\"http://xml.org/sax/features/external-parameter-entities\", false);\ndbf.setFeature(\"http://apache.org/xml/features/nonvalidating/load-external-dtd\", false);\ndbf.setXIncludeAware(false);\ndbf.setExpandEntityReferences(false);"
      },
      {
        "label": "Node.js fast-xml-parser: disable entity processing",
        "language": "javascript",
        "code": "import { XMLParser } from 'fast-xml-parser';\n\nconst parser = new XMLParser({\n  processEntities: false,\n  htmlEntities: false,\n});\n\nconst result = parser.parse(xmlBody);"
      }
    ],
    "references": [
      "https://owasp.org/www-community/vulnerabilities/XML_External_Entity_Processing",
      "https://cheatsheetseries.owasp.org/cheatsheets/XML_External_Entity_Prevention_Cheat_Sheet.html"
    ],
    "_idx": 21,
    "cwe": "CWE-611",
    "owasp": "A05:2021"
  },
  {
    "id": "api-soap-wsdl-publicly-accessible",
    "type": "combined",
    "title": "WSDL publicly accessible",
    "category": "api",
    "severity": "medium",
    "description": "Auto-published WSDL files enumerate every operation, parameter type, and binding - a blueprint for crafting SOAP-level attacks.",
    "evidence": "WSDL document is publicly accessible at {{url}}?wsdl without authentication.",
    "riskImpact": "A publicly accessible WSDL gives attackers a complete catalog of every SOAP operation, parameter name, type, and service binding, reducing the effort to discover injection points, craft malformed requests, and understand internal service architecture.",
    "explanation": "Auto-published WSDL files enumerate every operation, parameter type, and binding - a blueprint for crafting SOAP-level attacks.",
    "fixSteps": [
      "Restrict ?wsdl behind authentication or IP allowlisting",
      "Remove auto-publishing of WSDL from production endpoints; distribute via authenticated developer portals",
      "Consider whether external clients need WSDL at all, or if a static copy can be distributed out-of-band"
    ],
    "codeExamples": [
      {
        "label": "Nginx: restrict WSDL access to internal network",
        "language": "nginx",
        "code": "location ~* \\?wsdl {\n    allow 10.0.0.0/8;\n    allow 192.168.0.0/16;\n    deny all;\n    proxy_pass http://soap-backend;\n}"
      },
      {
        "label": "Apache CXF: disable WSDL auto-publishing",
        "language": "xml",
        "code": "<jaxws:endpoint id=\"myService\"\n    implementor=\"com.example.MyServiceImpl\"\n    address=\"/MyService\">\n  <jaxws:properties>\n    <entry key=\"publish-wsdl-information\" value=\"false\"/>\n    <entry key=\"org.apache.cxf.service.list.enabled\" value=\"false\"/>\n  </jaxws:properties>\n</jaxws:endpoint>"
      }
    ],
    "references": [
      "https://owasp.org/API-Security/editions/2023/en/0xa8-security-misconfiguration/",
      "https://cheatsheetseries.owasp.org/cheatsheets/Web_Service_Security_Cheat_Sheet.html"
    ],
    "_idx": 22,
    "cwe": "CWE-200",
    "owasp": "A05:2021"
  },
  {
    "id": "api-websocket-no-origin-validation",
    "type": "combined",
    "title": "WebSocket endpoint reachable, Origin handling unconfirmed",
    "category": "api",
    "severity": "low",
    "description": "A WebSocket-shaped endpoint was reachable and its response body doesn't reference Origin handling. This is a static content heuristic, not a live handshake test with a foreign Origin header, so it flags something to verify manually rather than a confirmed missing check.",
    "evidence": "WebSocket-shaped endpoint at {{url}} was reachable; its response body does not mention 'origin' - verify directly with the HTTP upgrade handler whether Origin is validated against an allowlist.",
    "riskImpact": "If the WebSocket handshake genuinely ignores the Origin header, any malicious website a victim visits can silently open an authenticated WebSocket connection using the victim's session cookies, then subscribe to their real-time events: a CSRF equivalent that is invisible to the user.",
    "explanation": "Origin validation happens server-side during the HTTP upgrade request and isn't observable in the page's static body content in either direction: a hardened server with a bare bootstrap page and an unhardened server whose unrelated JS happens to mention 'origin' would both be scored incorrectly by content alone.",
    "fixSteps": [
      "Validate the Origin header against an allowlist during the HTTP upgrade handshake",
      "Reject the connection with HTTP 403 if Origin is not in the allowlist",
      "Additionally require a CSRF token in the initial WS message for critical endpoints"
    ],
    "codeExamples": [
      {
        "label": "ws library: Origin validation on upgrade",
        "language": "javascript",
        "code": "import { WebSocketServer } from 'ws';\n\nconst ALLOWED_ORIGINS = new Set(['https://app.example.com']);\n\nconst wss = new WebSocketServer({\n  server: httpServer,\n  verifyClient({ origin }, callback) {\n    if (!origin || !ALLOWED_ORIGINS.has(origin)) {\n      callback(false, 403, 'Forbidden: origin not allowed');\n    } else {\n      callback(true);\n    }\n  },\n});"
      },
      {
        "label": "Socket.IO: built-in CORS origin check",
        "language": "javascript",
        "code": "import { Server } from 'socket.io';\n\nconst io = new Server(httpServer, {\n  cors: {\n    origin: ['https://app.example.com'],\n    methods: ['GET', 'POST'],\n    credentials: true,\n  },\n});"
      }
    ],
    "references": [
      "https://portswigger.net/web-security/websockets/cross-site-websocket-hijacking",
      "https://owasp.org/API-Security/editions/2023/en/0xa2-broken-authentication/"
    ],
    "_idx": 23,
    "cwe": "CWE-346",
    "owasp": "A05:2021"
  },
  {
    "id": "api-rest-mass-assignment-risk",
    "type": "body-pattern",
    "title": "REST endpoint response exposes privileged fields",
    "category": "api",
    "severity": "low",
    "description": "A response body contains privileged field names (role, isAdmin) that, if the same names are also accepted as unvalidated input, would let clients escalate privileges. Detected from response content only; this does not confirm the fields are writable.",
    "evidence": "API endpoint at {{url}} response body includes privileged field names (role/isAdmin) - verify these are not also accepted as request input without an allowlist.",
    "riskImpact": "Mass assignment lets an attacker send unexpected fields (role: 'admin', isVerified: true, creditBalance: 99999) in a JSON body and have them persisted directly to the database record, privilege escalation requiring only a crafted HTTP request.",
    "explanation": "Endpoints that bind a request body directly to a model (e.g., user.role, isAdmin, internalId) without an explicit allowlist let clients escalate privileges.",
    "fixSteps": [
      "Use explicit DTOs or field allowlists; never spread req.body directly onto ORM models",
      "Mark internal fields (role, isAdmin, isVerified) as non-writable in the schema",
      "Validate and strip unknown properties at the boundary using Zod, Joi, or similar"
    ],
    "codeExamples": [
      {
        "label": "Zod: strict DTO with unknown field rejection",
        "language": "javascript",
        "code": "import { z } from 'zod';\n\nconst UpdateUserDto = z.object({\n  name:      z.string().min(1).max(100).optional(),\n  bio:       z.string().max(500).optional(),\n  avatarUrl: z.string().url().optional(),\n  // role, isAdmin, isVerified are intentionally absent\n}).strict();  // .strict() rejects any extra fields\n\nrouter.put('/users/:id', requireAuth, async (req, res) => {\n  const parsed = UpdateUserDto.safeParse(req.body);\n  if (!parsed.success) {\n    return res.status(400).json({ errors: parsed.error.issues });\n  }\n  await db.users.update(req.params.id, parsed.data);\n  res.json({ success: true });\n});"
      }
    ],
    "references": [
      "https://cheatsheetseries.owasp.org/cheatsheets/Mass_Assignment_Cheat_Sheet.html",
      "https://owasp.org/API-Security/editions/2023/en/0xa3-broken-object-property-level-authorization/"
    ],
    "_idx": 24,
    "cwe": "CWE-915",
    "owasp": "A08:2021"
  },
  {
    "id": "rate-limiting",
    "type": "header",
    "title": "API endpoint has no rate limiting",
    "category": "api",
    "severity": "medium",
    "description": "The API endpoint does not enforce rate limiting, allowing unlimited requests from a single client.",
    "evidence": "No rate-limit headers (X-RateLimit-*, RateLimit-*) detected in responses from {{url}} after repeated requests.",
    "riskImpact": "Without rate limiting, attackers can send unlimited requests to brute-force authentication, scrape all data, enumerate resources, or trigger denial of service, all without any server-side throttling.",
    "explanation": "Rate limiting protects APIs from abuse by capping the number of requests a client can make in a time window. Without it, automated attacks against authentication, search, or data endpoints are unconstrained.",
    "fixSteps": [
      "Apply a rate-limit middleware to all API endpoints",
      "Key limits on the authenticated user ID for authed endpoints, or IP for anonymous ones",
      "Return 429 with Retry-After and RateLimit-* headers when the limit is hit"
    ],
    "codeExamples": [
      {
        "label": "express-rate-limit: basic setup",
        "language": "javascript",
        "code": "import rateLimit from 'express-rate-limit';\n\nconst limiter = rateLimit({\n  windowMs: 60 * 1000,\n  max: 100,\n  standardHeaders: true,\n  legacyHeaders: false,\n});\n\napp.use('/api/', limiter);"
      },
      {
        "label": "Nginx: rate limiting with burst",
        "language": "nginx",
        "code": "http {\n    limit_req_zone $binary_remote_addr zone=api:10m rate=60r/m;\n\n    server {\n        location /api/ {\n            limit_req zone=api burst=20 nodelay;\n            limit_req_status 429;\n        }\n    }\n}"
      }
    ],
    "references": [
      "https://owasp.org/API-Security/editions/2023/en/0xa4-unrestricted-resource-consumption/",
      "https://datatracker.ietf.org/doc/html/draft-ietf-httpapi-ratelimit-headers"
    ],
    "cwe": "CWE-799",
    "owasp": "A04:2021",
    "_idx": 25
  },
  {
    "id": "options-method-exposed",
    "type": "header",
    "title": "OPTIONS response exposes state-changing HTTP methods",
    "category": "api",
    "severity": "info",
    "description": "The OPTIONS response Allow header lists a state-changing method (TRACE, DELETE, or PUT), one of the higher-risk verbs to have reachable if the corresponding handler isn't properly authenticated. This is distinct from the separate 'full method allowlist' finding, which flags on verb count rather than which specific verbs are present.",
    "evidence": "OPTIONS request to {{url}} returned an Allow header listing a state-changing method: {{evidence}}",
    "riskImpact": "Confirming TRACE, DELETE, or PUT are routed on this endpoint tells attackers exactly which higher-risk verbs to prioritize when probing for missing authentication or input validation.",
    "explanation": "An HTTP OPTIONS request returns the Allow header listing all methods the server accepts on that path. This check flags when a specific higher-risk verb (TRACE, DELETE, PUT) is present, regardless of how many total methods are listed.",
    "fixSteps": [
      "Restrict the Allow header to only methods that endpoint actually serves",
      "Disable OPTIONS on endpoints that do not need CORS",
      "Return 405 for any method not in the Allow list"
    ],
    "codeExamples": [
      {
        "label": "Express: explicit Allow header per route",
        "language": "javascript",
        "code": "router.all('/resource', (req, res, next) => {\n  if (req.method === 'OPTIONS') {\n    return res.set('Allow', 'GET, POST').status(204).end();\n  }\n  next();\n});"
      },
      {
        "label": "Nginx: restrict HTTP methods",
        "language": "nginx",
        "code": "location /api/resource {\n    limit_except GET POST OPTIONS {\n        deny all;\n    }\n}"
      }
    ],
    "references": [
      "https://httpwg.org/specs/rfc9110.html#OPTIONS",
      "https://owasp.org/API-Security/editions/2023/en/0xa8-security-misconfiguration/"
    ],
    "cwe": "CWE-200",
    "owasp": "A05:2021",
    "_idx": 26
  },
  {
    "id": "soap-endpoint",
    "type": "body-pattern",
    "title": "SOAP endpoint detected",
    "category": "api",
    "severity": "info",
    "description": "A SOAP/XML web service endpoint was detected. SOAP services have a unique attack surface including XXE, SSRF via SOAPAction, and schema enumeration via WSDL.",
    "evidence": "SOAP envelope or Content-Type: text/xml response detected at {{url}}.",
    "riskImpact": "SOAP endpoints are susceptible to XML External Entity (XXE) attacks, SOAPAction injection leading to SSRF, and schema enumeration via publicly accessible WSDL files: attacks that can exfiltrate files, pivot to internal networks, or reveal internal service architecture.",
    "explanation": "SOAP services use XML for messages and are susceptible to a class of vulnerabilities distinct from REST APIs: XXE, SOAPAction injection, WSDL enumeration, and WS-Security misconfigurations.",
    "fixSteps": [
      "Disable DOCTYPE and external entity processing in the XML parser",
      "Restrict ?wsdl access to authenticated or internal clients only",
      "Validate SOAPAction against a strict allowlist of known operations",
      "Enable WS-Security for authentication and message integrity"
    ],
    "codeExamples": [
      {
        "label": "Java: XXE-safe SAX parser factory",
        "language": "java",
        "code": "SAXParserFactory spf = SAXParserFactory.newInstance();\nspf.setFeature(\"http://apache.org/xml/features/disallow-doctype-decl\", true);\nspf.setFeature(\"http://xml.org/sax/features/external-general-entities\", false);\nspf.setFeature(\"http://xml.org/sax/features/external-parameter-entities\", false);\nspf.setNamespaceAware(true);"
      }
    ],
    "references": [
      "https://cheatsheetseries.owasp.org/cheatsheets/XML_External_Entity_Prevention_Cheat_Sheet.html",
      "https://cheatsheetseries.owasp.org/cheatsheets/Web_Service_Security_Cheat_Sheet.html"
    ],
    "cwe": "CWE-611",
    "owasp": "A05:2021",
    "_idx": 27
  },
  {
    "id": "xml-rpc",
    "type": "body-pattern",
    "title": "XML-RPC endpoint referenced",
    "category": "api",
    "severity": "low",
    "description": "A page references an XML-RPC endpoint (e.g. xmlrpc.php). In WordPress and similar CMSes this legacy endpoint enables brute-force amplification attacks and remote method invocation when it's actually reachable and un-hardened.",
    "evidence": "XML-RPC reference found at {{url}}. This is a text match on the response content, not a probe to the endpoint itself; confirm the endpoint is actually reachable before treating this as exposure.",
    "riskImpact": "WordPress xmlrpc.php allows an attacker to test thousands of username/password combinations in a single HTTP request using system.multicall: a brute-force amplification attack. It also enables pingback-based SSRF and DDoS amplification against third-party hosts.",
    "explanation": "XML-RPC is a legacy remote procedure call protocol used by WordPress for the mobile app and Jetpack. When left publicly accessible it enables brute-force amplification (one request = thousands of auth attempts), pingback SSRF, and remote content modification.",
    "fixSteps": [
      "In WordPress: disable xmlrpc.php unless specifically required by a plugin or mobile app",
      "Block xmlrpc.php at the web server or WAF level for non-WordPress applications",
      "Use a security plugin (Wordfence, iThemes Security) to block XML-RPC at the application level"
    ],
    "codeExamples": [
      {
        "label": "Nginx: block xmlrpc.php",
        "language": "nginx",
        "code": "location = /xmlrpc.php {\n    deny all;\n    return 403;\n}"
      },
      {
        "label": "Apache: block xmlrpc.php",
        "language": "apache",
        "code": "<Files xmlrpc.php>\n    Order Deny,Allow\n    Deny from all\n</Files>"
      },
      {
        "label": "WordPress: disable XML-RPC via filter",
        "language": "php",
        "code": "// In functions.php or a must-use plugin\nadd_filter('xmlrpc_enabled', '__return_false');\n\nadd_filter('xmlrpc_methods', function($methods) {\n    unset($methods['pingback.ping']);\n    unset($methods['pingback.extensions.getPingbacks']);\n    return $methods;\n});"
      }
    ],
    "references": [
      "https://owasp.org/www-project-web-security-testing-guide/v42/4-Web_Application_Security_Testing/10-Business_Logic_Testing/",
      "https://wordpress.org/documentation/article/xml-rpc-support/"
    ],
    "cwe": "CWE-611",
    "owasp": "A05:2021",
    "_idx": 28
  },
  {
    "id": "trace-method-enabled",
    "type": "header",
    "title": "HTTP TRACE method enabled",
    "category": "api",
    "severity": "medium",
    "description": "HTTP TRACE method is enabled on the server. TRACE reflects the full request including headers, enabling Cross-Site Tracing (XST) attacks.",
    "evidence": "Response headers for {{url}} include an Allow header listing TRACE. This is a passive header observation, not a live TRACE request; confirm the server actually reflects the request body before treating this as exploitable XST.",
    "riskImpact": "Cross-Site Tracing (XST) exploits TRACE to reflect the victim's cookies and Authorization headers, including HttpOnly cookies that JavaScript cannot read, back through the browser in a cross-origin context, enabling session theft even when HttpOnly is correctly set.",
    "explanation": "HTTP TRACE is intended for diagnostic loop-back testing but is rarely needed in production. When enabled, it can be exploited via XST to steal credentials from authenticated users.",
    "fixSteps": [
      "Disable TRACE at the web server or reverse proxy level",
      "In Apache: add TraceEnable Off to httpd.conf",
      "In Nginx: return 405 for TRACE requests",
      "In Express: reject TRACE in a global middleware before routing"
    ],
    "codeExamples": [
      {
        "label": "Nginx: disable TRACE",
        "language": "nginx",
        "code": "server {\n    if ($request_method = TRACE) {\n        return 405;\n    }\n}"
      },
      {
        "label": "Apache: TraceEnable Off",
        "language": "apache",
        "code": "# In httpd.conf\nTraceEnable Off"
      },
      {
        "label": "Express.js: reject TRACE globally",
        "language": "javascript",
        "code": "app.use((req, res, next) => {\n  if (req.method === 'TRACE') {\n    return res.status(405).set('Allow', 'GET, POST, PUT, DELETE').end();\n  }\n  next();\n});"
      }
    ],
    "references": [
      "https://owasp.org/www-community/attacks/Cross_Site_Tracing",
      "https://httpwg.org/specs/rfc9110.html#TRACE"
    ],
    "cwe": "CWE-16",
    "owasp": "A05:2021",
    "_idx": 29
  },
  {
    "id": "api-jwt-jku-x5u-header-claim",
    "type": "combined",
    "title": "JWT header declares a jku or x5u key-location claim",
    "category": "api",
    "severity": "medium",
    "description": "A JSON Web Token found in the response body or a Set-Cookie header decodes to a header segment containing a jku (JWK Set URL) or x5u (X.509 URL) claim that points to an external URL, meaning whichever server verifies this token may fetch the verification key from a location named inside the token itself.",
    "evidence": "A JWT found at {{url}} decodes to a header declaring {{evidence}} - confirm the verifying server validates that URL against an allowlisted host rather than trusting it as given.",
    "riskImpact": "If the verification code resolves the signing key from the jku/x5u URL without checking it against an allowlist, an attacker can host their own key pair, sign a token with their own private key, and point jku/x5u at their own JWKS endpoint. The server then verifies the attacker-forged token successfully because it fetched the attacker's own public key to check it against.",
    "explanation": "The token's header segment is base64url-decoded directly from a token found in the page or a cookie value, so this confirms the claim's literal presence in an issued token, not that the server's verification logic actually trusts it unvalidated. Many JWT libraries require jku/x5u handling to be wired up explicitly by the developer, so presence of the claim alone does not confirm the vulnerable code path exists.",
    "fixSteps": [
      "Never resolve jku or x5u directly from an untrusted token; validate the URL's host against an explicit allowlist before fetching anything",
      "Prefer distributing keys out-of-band, a fixed JWKS endpoint your own code controls or an embedded public key, instead of trusting the token to name its own key source",
      "If jku support is required, pin the set of allowed hosts and reject any URL that does not match"
    ],
    "codeExamples": [
      {
        "label": "Node.js (jose): allowlist the jku host before fetching",
        "language": "javascript",
        "code": "import { jwtVerify, createRemoteJWKSet, decodeProtectedHeader } from 'jose';\n\nconst ALLOWED_JKU_HOSTS = new Set(['keys.example.com']);\n\nfunction resolveJwks(token) {\n  const header = decodeProtectedHeader(token);\n  if (typeof header.jku !== 'string') {\n    throw new Error('No jku present');\n  }\n  const jkuUrl = new URL(header.jku);\n  if (!ALLOWED_JKU_HOSTS.has(jkuUrl.hostname)) {\n    throw new Error('Untrusted jku host: ' + jkuUrl.hostname);\n  }\n  return createRemoteJWKSet(jkuUrl);\n}"
      },
      {
        "label": "Safer: ignore jku/x5u entirely, verify against a fixed JWKS endpoint",
        "language": "javascript",
        "code": "import { jwtVerify, createRemoteJWKSet } from 'jose';\n\n// Always fetch keys from your own known JWKS endpoint,\n// never from a URL named inside the token being verified.\nconst JWKS = createRemoteJWKSet(\n  new URL('https://auth.example.com/.well-known/jwks.json'),\n);\n\nconst { payload } = await jwtVerify(token, JWKS);"
      }
    ],
    "references": [
      "https://datatracker.ietf.org/doc/html/rfc7515#section-4.1.2",
      "https://www.rfc-editor.org/rfc/rfc8725#section-3.7"
    ],
    "_idx": 30,
    "cwe": "CWE-347",
    "owasp": "A02:2021"
  },
  {
    "id": "api-oauth-authorize-missing-pkce",
    "type": "url-check",
    "title": "OAuth authorization code request missing PKCE",
    "category": "api",
    "severity": "medium",
    "description": "A request made to an OAuth/OIDC authorize endpoint during the scan uses response_type=code with a client_id but no code_challenge parameter, meaning the authorization code flow is running without Proof Key for Code Exchange (PKCE).",
    "evidence": "Authorization request to {{url}} uses response_type=code with a client_id but no code_challenge parameter.",
    "riskImpact": "Without PKCE, an authorization code intercepted in transit, via a malicious app registered on the same custom redirect URI scheme, a compromised network, or referrer leakage from the redirect target, can be exchanged for tokens by whoever captures it. PKCE binds the code to the client that originated the request, so an intercepted code alone is no longer enough to redeem it.",
    "explanation": "This inspects the actual request URL fetched during the scan for the OAuth authorize endpoint shape (response_type=code, a client_id, and the presence or absence of code_challenge); it is not a text match against page content. It cannot tell whether this OAuth client is confidential (a server-side app exchanging the code with a client_secret) or public (an SPA or native app); PKCE matters most for public clients but is now recommended for all client types.",
    "fixSteps": [
      "Generate a random code_verifier and derive code_challenge from it with S256 before redirecting to the authorize endpoint",
      "Send code_challenge and code_challenge_method=S256 as parameters on the authorize request",
      "Send the original code_verifier when exchanging the authorization code at the token endpoint"
    ],
    "codeExamples": [
      {
        "label": "Build a PKCE-enabled authorize redirect",
        "language": "javascript",
        "code": "async function buildAuthorizeUrl() {\n  const verifier = base64url(crypto.getRandomValues(new Uint8Array(32)));\n  sessionStorage.setItem('pkce_verifier', verifier);\n\n  const challengeBytes = await crypto.subtle.digest(\n    'SHA-256',\n    new TextEncoder().encode(verifier),\n  );\n  const challenge = base64url(new Uint8Array(challengeBytes));\n\n  const url = new URL('https://idp.example.com/oauth2/v1/authorize');\n  url.searchParams.set('response_type', 'code');\n  url.searchParams.set('client_id', CLIENT_ID);\n  url.searchParams.set('redirect_uri', REDIRECT_URI);\n  url.searchParams.set('code_challenge', challenge);\n  url.searchParams.set('code_challenge_method', 'S256');\n  url.searchParams.set('state', crypto.randomUUID());\n  return url.toString();\n}"
      }
    ],
    "references": [
      "https://datatracker.ietf.org/doc/html/rfc7636",
      "https://datatracker.ietf.org/doc/html/rfc9700"
    ],
    "_idx": 31,
    "cwe": "CWE-294",
    "owasp": "A07:2021"
  },
  {
    "id": "api-oauth-implicit-flow-response-type-token",
    "type": "url-check",
    "title": "OAuth implicit grant (response_type=token) in use",
    "category": "api",
    "severity": "medium",
    "description": "A request made to an OAuth/OIDC authorize endpoint during the scan uses response_type=token (with no code value present), the deprecated implicit grant that returns the access token directly in the redirect URL fragment instead of through a back-channel exchange.",
    "evidence": "Authorization request to {{url}} uses response_type={{evidence}}, the OAuth implicit grant.",
    "riskImpact": "The implicit grant returns the access token in the fragment of a browser redirect, where it can leak via browser history, via the Referer header on any subsequent same-tab navigation that doesn't strip fragments, and to any script or browser extension with access to the page. It also gives the client no way to authenticate itself during the exchange, unlike the code flow's token endpoint call.",
    "explanation": "This inspects the actual request URL fetched during the scan for the OAuth authorize endpoint shape and a response_type value that includes token without also including code (a pure implicit or token-only hybrid request); a code+token hybrid request still uses an authorization code for the code portion and is left to the separate PKCE check.",
    "fixSteps": [
      "Migrate to the authorization code flow with PKCE; the implicit grant is removed entirely from OAuth 2.1 and disallowed by RFC 9700 for every client type",
      "For single-page apps, use a PKCE-enabled code flow with the token exchange happening in JavaScript, not response_type=token"
    ],
    "codeExamples": [
      {
        "label": "Migrate implicit grant to code + PKCE",
        "language": "javascript",
        "code": "// Before (implicit grant, deprecated):\n// url.searchParams.set('response_type', 'token');\n\n// After (authorization code + PKCE):\nurl.searchParams.set('response_type', 'code');\nurl.searchParams.set('code_challenge', challenge);\nurl.searchParams.set('code_challenge_method', 'S256');\n// Exchange the returned code for tokens server-side or via a\n// confidential token-exchange call, never in the URL fragment."
      }
    ],
    "references": [
      "https://datatracker.ietf.org/doc/html/rfc9700",
      "https://oauth.net/2/grant-types/implicit/"
    ],
    "_idx": 32,
    "cwe": "CWE-522",
    "owasp": "A07:2021"
  },
  {
    "id": "api-verbose-error-internal-path",
    "type": "combined",
    "title": "API error response leaks a stack trace or internal file path",
    "category": "api",
    "severity": "medium",
    "description": "A JSON API error response includes a \"stack\" field containing a real stack trace, or a \"message\" field referencing an internal filesystem path, exposing implementation details to any caller who triggers the error.",
    "evidence": "{{evidence}}",
    "riskImpact": "Stack traces and internal paths reveal the framework and version in use, the deployment directory layout, and internal module structure: the reconnaissance an attacker needs to fingerprint the stack and look up known vulnerabilities for that exact version, or to craft path traversal and local file inclusion payloads with a real starting path.",
    "explanation": "The response must actually be JSON-shaped (an application/json content type or an /api/ URL path) and the offending value must sit inside a genuine \"stack\" or \"message\" JSON field, not any file-path-looking text elsewhere on the page. A roughly 200-character lookback before the match excludes content preceded by a fenced code block, <code>/<pre>, or the words example/documentation, so a blog post or docs page discussing this exact vulnerability in prose does not self-trigger it.",
    "fixSteps": [
      "Never send a raw Error.stack or native driver error message to the client; log it server-side and return a generic message plus an internal error id for support lookups",
      "Centralize error handling at the framework boundary (an Express error-handling middleware, a Next.js error handler) so no individual route can leak an unhandled exception",
      "Add a test that exercises a forced 500 response and asserts the body contains no file paths or stack frames"
    ],
    "codeExamples": [
      {
        "label": "Express: centralized error handler that strips internals",
        "language": "javascript",
        "code": "app.use((err, req, res, next) => {\n  console.error(err); // full detail stays server-side\n\n  const isProd = process.env.NODE_ENV === 'production';\n  res.status(err.status || 500).json({\n    error: isProd ? 'Internal server error' : err.message,\n    requestId: req.id,\n  });\n});"
      }
    ],
    "references": [
      "https://cheatsheetseries.owasp.org/cheatsheets/Error_Handling_Cheat_Sheet.html",
      "https://owasp.org/Top10/A05_2021-Security_Misconfiguration/"
    ],
    "_idx": 33,
    "cwe": "CWE-209",
    "owasp": "A05:2021"
  },
  {
    "id": "api-deprecation-header-missing",
    "type": "combined",
    "title": "API reports itself deprecated without a Deprecation/Sunset header",
    "category": "api",
    "severity": "low",
    "description": "A JSON API response explicitly reports itself as deprecated, a deprecated: true field, or a status/message field saying so, but the HTTP response carries neither the standard Deprecation nor Sunset header.",
    "evidence": "Response body at {{url}} reports this endpoint/version as deprecated, but no Deprecation or Sunset response header is present.",
    "riskImpact": "Without the standard headers, API gateways, generated client SDKs, and monitoring tooling that watch for RFC 8594/9745 deprecation signals have no machine-readable way to detect and alert on the deprecation. Only a developer who happens to read the response body notices, so integrations keep shipping against an endpoint on its way out with no advance warning built into their tooling.",
    "explanation": "Gated on the response actually being JSON-shaped (an application/json content type, or a body that starts with { or [), so an ordinary HTML documentation page whose prose happens to mention deprecation does not trigger this.",
    "fixSteps": [
      "Add a Deprecation response header (RFC 9745), either Deprecation: true or a deprecation date, alongside the existing in-body notice",
      "Add a Sunset response header (RFC 8594) with the planned removal date once one is set",
      "Keep the in-body notice too so it stays visible to callers who don't inspect headers"
    ],
    "codeExamples": [
      {
        "label": "Express: RFC-compliant deprecation headers",
        "language": "javascript",
        "code": "router.get('/v1/users', (req, res) => {\n  res.set('Deprecation', 'true');\n  res.set('Sunset', 'Wed, 01 Apr 2026 00:00:00 GMT');\n  res.set('Link', '</v2/users>; rel=\"successor-version\"');\n  res.json({ deprecated: true, data: getUsersV1() });\n});"
      }
    ],
    "references": [
      "https://datatracker.ietf.org/doc/html/rfc8594",
      "https://datatracker.ietf.org/doc/html/rfc9745"
    ],
    "_idx": 34
  },
  {
    "id": "api-graphql-introspection-mutation-heavy",
    "type": "body-pattern",
    "title": "GraphQL introspection exposes a mutation-heavy schema",
    "category": "api",
    "severity": "high",
    "description": "The GraphQL introspection response doesn't just confirm introspection is enabled, it resolves a \"Mutation\" root type with five or more distinct mutation fields, meaning the entire write surface of the API, not only its read schema, is enumerable by any caller.",
    "evidence": "GraphQL introspection at {{url}} resolves a \"Mutation\" type exposing {{evidence}}.",
    "riskImpact": "An attacker gets the exact name, arguments, and return type of every mutation, createUser, deleteAccount, grantRole, whatever exists, without probing or guessing field names first. A data-model leak becomes a ready-made target list of every write operation the API exposes, prioritizing which ones to attack first.",
    "explanation": "Builds on the base introspection-enabled check by requiring an actually resolved schema (a real \"__schema\" object, not just the introspection query text) and specifically locating the \"Mutation\" OBJECT type's own \"fields\" array. Field names are counted only at that array's top level, distinguishing genuine mutation field entries from the \"name\" keys that also appear on each field's nested argument and return-type objects, so the count reflects real mutations rather than incidental \"name\" occurrences.",
    "fixSteps": [
      "Disable introspection in production (Apollo: introspection: false, GraphQL.NET: ExposeMetadata = false)",
      "If internal tooling needs introspection, gate it behind authentication rather than leaving it open to any caller",
      "Review the full mutation list for operations that should never have been reachable without authorization (admin-only actions, destructive operations) and add field-level auth checks"
    ],
    "codeExamples": [
      {
        "label": "Apollo Server 4: disable introspection in production",
        "language": "javascript",
        "code": "import { ApolloServer } from '@apollo/server';\n\nconst server = new ApolloServer({\n  typeDefs,\n  resolvers,\n  introspection: process.env.NODE_ENV !== 'production',\n});"
      }
    ],
    "references": [
      "https://graphql.org/learn/introspection/",
      "https://owasp.org/API-Security/editions/2023/en/0xa8-security-misconfiguration/"
    ],
    "_idx": 35,
    "cwe": "CWE-200",
    "owasp": "A05:2021"
  },
  {
    "id": "api-graphql-ide-exposed",
    "type": "body-pattern",
    "title": "GraphQL IDE served in production",
    "category": "api",
    "severity": "medium",
    "description": "This endpoint serves an interactive GraphQL IDE: a schema browser and a query editor for this API. GraphiQL, GraphQL Playground, Apollo Sandbox, Voyager and the Hasura console are all development tools, and every one of them is meant to be switched off outside development.",
    "evidence": "The response body at {{url}} loads an interactive GraphQL IDE bundle.",
    "riskImpact": "The IDE gives anyone who finds the endpoint a full schema browser plus a request builder pointed at your production API, with your visitor's own cookies attached. Enumerating the write surface, discovering fields the frontend never calls, and iterating on an authorization bypass all stop needing any tooling of the attacker's own.",
    "explanation": "The detector matches structural markers only: the IDE's own bundle filename, its document title, or the function that renders it (GraphiQL.createFetcher, renderGraphiQL, ApolloServerPluginLandingPage). A page that mentions these products in prose does not match, so documentation and marketing pages do not fire it. Note that serving the IDE and enabling introspection are separate switches: turning off one does not turn off the other.",
    "fixSteps": [
      "Disable the IDE plugin in production. In Apollo Server, pass ApolloServerPluginLandingPageDisabled() rather than relying on NODE_ENV alone.",
      "In express-graphql / graphql-http, set graphiql to false when the environment is not development.",
      "For Hasura, do not deploy with HASURA_GRAPHQL_ENABLE_CONSOLE=true; run the console locally against the remote endpoint instead.",
      "Disable schema introspection at the same time, since the IDE and introspection are controlled independently."
    ],
    "codeExamples": [
      {
        "label": "Apollo Server: disable the landing page in production",
        "language": "typescript",
        "code": "import {\n  ApolloServerPluginLandingPageDisabled,\n  ApolloServerPluginLandingPageLocalDefault,\n} from '@apollo/server/plugin/landingPage/default';\n\nconst isProd = process.env.NODE_ENV === 'production';\n\nnew ApolloServer({\n  schema,\n  introspection: !isProd,\n  plugins: [\n    isProd\n      ? ApolloServerPluginLandingPageDisabled()\n      : ApolloServerPluginLandingPageLocalDefault(),\n  ],\n});"
      },
      {
        "label": "graphql-http: gate GraphiQL on the environment",
        "language": "javascript",
        "code": "app.all(\n  '/graphql',\n  createHandler({\n    schema,\n    graphiql: process.env.NODE_ENV !== 'production',\n  }),\n);"
      }
    ],
    "references": [
      "https://www.apollographql.com/docs/apollo-server/api/plugin/landing-pages/",
      "https://cheatsheetseries.owasp.org/cheatsheets/GraphQL_Cheat_Sheet.html"
    ],
    "cwe": "CWE-489",
    "owasp": "A05:2021",
    "_idx": 36
  },
  {
    "id": "api-graphql-schema-sdl-exposed",
    "type": "body-pattern",
    "title": "GraphQL schema served as raw SDL",
    "category": "api",
    "severity": "low",
    "description": "The response is a GraphQL schema document in Schema Definition Language form, served as plain text. That is the schema itself, not an HTML page that happens to show one.",
    "evidence": "The response at {{url}} is not HTML and contains a GraphQL SDL type definition block.",
    "riskImpact": "An SDL dump hands over the same map that introspection would, without needing introspection to be enabled. Every type, field, argument, enum value and input object is readable, including internal fields the public client never queries, which is the starting point for finding an operation whose authorization was never written.",
    "explanation": "Disabling introspection is a common hardening step, and it is quietly undone when the build also publishes schema.graphql next to the bundle. The detector requires the response not to be HTML and to contain a real type Query { ... } block plus a second SDL construct (a Mutation type, a schema block, an input, or an enum), so a tutorial page showing a schema in a code block does not match.",
    "fixSteps": [
      "Remove the .graphql / .gql schema file from anything the web server can serve, and from the published build output.",
      "If a schema artifact is needed by CI or by a client codegen step, publish it to a private registry or an authenticated endpoint instead of the public origin.",
      "Confirm introspection is disabled too: an attacker only needs one of the two paths."
    ],
    "codeExamples": [
      {
        "label": "Next.js: keep schema files out of the public directory",
        "language": "bash",
        "code": "# Schema files belong beside the server code, not under public/\nmv public/schema.graphql server/schema.graphql\n\n# Verify nothing else ships one\ngrep -rl 'type Query' .next/static public 2>/dev/null"
      },
      {
        "label": "Nginx: refuse to serve schema artifacts",
        "language": "nginx",
        "code": "location ~* \\.(graphql|gql)$ {\n    return 404;\n}"
      }
    ],
    "references": [
      "https://cheatsheetseries.owasp.org/cheatsheets/GraphQL_Cheat_Sheet.html",
      "https://spec.graphql.org/October2021/#sec-Type-System"
    ],
    "cwe": "CWE-540",
    "owasp": "A01:2021",
    "_idx": 37
  },
  {
    "id": "api-openapi-no-security-declared",
    "type": "body-pattern",
    "title": "OpenAPI document declares no security for write operations",
    "category": "api",
    "severity": "low",
    "description": "The served OpenAPI document describes write operations but declares no security scheme anywhere. There is no securitySchemes block, no securityDefinitions, and no security requirement on any POST, PUT, PATCH or DELETE operation.",
    "evidence": "OpenAPI document at {{url}} describes state-changing operations and declares no security scheme or requirement.",
    "riskImpact": "The contract clients and gateways generate from this document says every write is anonymous. Tooling that enforces the spec (an API gateway, a mock server, a generated SDK) will not attach credentials, and a reviewer reading the document has no way to tell an intentional public API from an endpoint whose auth was never declared.",
    "explanation": "This reads the published document, not the running server: an operation can still be protected by middleware that the spec never mentions. That gap is the finding. A spec that omits security is either describing a genuinely public API, in which case an explicit empty security requirement says so, or it has drifted from the implementation. Read-only documents are not flagged, only ones that describe writes.",
    "fixSteps": [
      "Declare the scheme once under components.securitySchemes, then apply it with a top-level security requirement so every operation inherits it.",
      "Override the inherited requirement per operation with security: [] on the endpoints that really are public, so the exception is explicit.",
      "Add a spec lint step to CI (Spectral's owasp ruleset covers this) so a new operation cannot merge without a declared requirement."
    ],
    "codeExamples": [
      {
        "label": "OpenAPI 3: declare and apply a bearer scheme",
        "language": "yaml",
        "code": "components:\n  securitySchemes:\n    bearerAuth:\n      type: http\n      scheme: bearer\n      bearerFormat: JWT\n\nsecurity:\n  - bearerAuth: []\n\npaths:\n  /health:\n    get:\n      security: []   # deliberately public\n      responses:\n        '200': { description: OK }"
      },
      {
        "label": "CI: fail the build on an undeclared requirement",
        "language": "bash",
        "code": "npx @stoplight/spectral-cli lint openapi.yaml \\\n  --ruleset https://unpkg.com/@stoplight/spectral-owasp-ruleset/dist/ruleset.js \\\n  --fail-severity=error"
      }
    ],
    "references": [
      "https://spec.openapis.org/oas/v3.1.0#security-requirement-object",
      "https://owasp.org/API-Security/editions/2023/en/0xa5-broken-function-level-authorization/"
    ],
    "cwe": "CWE-306",
    "owasp": "A01:2021",
    "_idx": 38
  },
  {
    "id": "api-openapi-server-url-plain-http",
    "type": "body-pattern",
    "title": "OpenAPI document publishes a cleartext base URL",
    "category": "api",
    "severity": "medium",
    "description": "The served API description publishes a public base URL over plain http rather than https. That is either an OpenAPI 3 servers entry, or a Swagger 2.0 schemes list containing http and not https.",
    "evidence": "API description at {{url}} publishes an http:// base URL for a public host.",
    "riskImpact": "Every SDK, mock server, and gateway generated from this document targets the cleartext URL by default. Requests built that way carry the API key or bearer token in plaintext over the network, and an on-path attacker can read the credential and rewrite the response before the client sees it.",
    "explanation": "This is specifically about a publicly routable http:// base URL. Localhost, RFC1918 and staging or internal hostnames are excluded here because those are reported separately as an internal-server-URL leak. A redirect from :80 to :443 does not make this safe: the first request, with its Authorization header, has already been sent in the clear.",
    "fixSteps": [
      "Change every servers[].url in the document to https://.",
      "For Swagger 2.0, set schemes: [https] and remove http.",
      "Regenerate and republish any SDKs built from the old document, since the base URL is usually baked into the generated client."
    ],
    "codeExamples": [
      {
        "label": "OpenAPI 3: https base URLs only",
        "language": "yaml",
        "code": "servers:\n  - url: https://api.example.com/v1\n    description: Production\n  - url: https://sandbox.example.com/v1\n    description: Sandbox"
      },
      {
        "label": "Swagger 2.0: drop the http scheme",
        "language": "yaml",
        "code": "swagger: '2.0'\nhost: api.example.com\nbasePath: /v1\nschemes:\n  - https"
      }
    ],
    "references": [
      "https://spec.openapis.org/oas/v3.1.0#server-object",
      "https://owasp.org/API-Security/editions/2023/en/0xa8-security-misconfiguration/"
    ],
    "cwe": "CWE-319",
    "owasp": "A02:2021",
    "_idx": 39
  },
  {
    "id": "api-openapi-swagger-2-document",
    "type": "body-pattern",
    "title": "API described with Swagger 2.0",
    "category": "api",
    "severity": "info",
    "description": "The API description served here declares swagger: 2.0. That format was superseded by OpenAPI 3.0 in 2017 and by 3.1 in 2021.",
    "evidence": "API description at {{url}} declares \"swagger\": \"2.0\" and contains a paths object.",
    "riskImpact": "Swagger 2.0 cannot express several things a security reviewer needs. It has no cookie parameter location, no way to describe more than one server, no oneOf/anyOf in schemas, and its OAuth2 model predates PKCE. Constraints that are not expressible are constraints that generated clients and gateways will not enforce.",
    "explanation": "This is informational, not a vulnerability. It is worth knowing because the surrounding tooling tends to be the same age as the document: a Swagger 2.0 spec is usually served by a Swagger UI 2.x or 3.x bundle, and validated by a generator that has not shipped a security fix in years. Converting is largely mechanical.",
    "fixSteps": [
      "Convert the document with swagger2openapi, then review the securitySchemes section by hand.",
      "Move the host / basePath / schemes triple into a servers array.",
      "Upgrade the Swagger UI or Redoc bundle serving it at the same time."
    ],
    "codeExamples": [
      {
        "label": "Convert to OpenAPI 3",
        "language": "bash",
        "code": "npx swagger2openapi swagger.json -o openapi.json --resolve\n\n# Then validate the result\nnpx @redocly/cli lint openapi.json"
      }
    ],
    "references": [
      "https://spec.openapis.org/oas/v3.1.0",
      "https://swagger.io/docs/specification/about/"
    ],
    "cwe": "CWE-1104",
    "owasp": "A06:2021",
    "_idx": 40
  },
  {
    "id": "api-openapi-deprecated-operations-exposed",
    "type": "body-pattern",
    "title": "OpenAPI document advertises deprecated operations",
    "category": "api",
    "severity": "info",
    "description": "The served API description marks one or more operations or parameters as deprecated. They are still published as callable, so the contract advertises a route the team has already written off.",
    "evidence": "OpenAPI document at {{url}} contains \"deprecated\": true entries.",
    "riskImpact": "A deprecated route is usually the one that stopped getting attention: it keeps the old parameter handling, the old validation, and often the old authorization code, while review and test effort moves to its replacement. Publishing it in the contract tells an attacker exactly which path that is.",
    "explanation": "Marking an operation deprecated is good practice and is not itself a problem. The finding is the pairing: the document says do not use this, and the server still serves it. Treat the deprecated list as a work queue with a removal date, and pair it with a Deprecation and Sunset header on the live responses so clients find out at runtime rather than by reading the spec.",
    "fixSteps": [
      "Give every deprecated operation a removal date and track it.",
      "Emit Deprecation and Sunset headers on those routes so callers see the notice without reading the spec.",
      "Log calls to deprecated routes with the caller identity, so you know who still depends on them before removal.",
      "Delete the route and its spec entry once the sunset date passes."
    ],
    "codeExamples": [
      {
        "label": "OpenAPI 3: deprecate with a stated replacement",
        "language": "yaml",
        "code": "paths:\n  /v1/users/search:\n    get:\n      deprecated: true\n      summary: 'Deprecated: use POST /v2/users/query. Removal 2027-01-01.'\n      responses:\n        '200': { description: OK }"
      },
      {
        "label": "Express: announce the sunset at runtime",
        "language": "javascript",
        "code": "router.get('/v1/users/search', (req, res, next) => {\n  res.set('Deprecation', 'true');\n  res.set('Sunset', 'Fri, 01 Jan 2027 00:00:00 GMT');\n  res.set('Link', '</v2/users/query>; rel=\"successor-version\"');\n  next();\n});"
      }
    ],
    "references": [
      "https://spec.openapis.org/oas/v3.1.0#operation-object",
      "https://datatracker.ietf.org/doc/html/rfc9745"
    ],
    "cwe": "CWE-1059",
    "owasp": "A09:2021",
    "_idx": 41
  },
  {
    "id": "api-openapi-oauth2-implicit-flow-declared",
    "type": "body-pattern",
    "title": "OpenAPI document declares the OAuth2 implicit flow",
    "category": "api",
    "severity": "medium",
    "description": "The served API description declares an OAuth2 security scheme using the implicit grant. That flow returns the access token directly in the redirect URL fragment instead of through a back-channel exchange.",
    "evidence": "API description at {{url}} declares an OAuth2 implicit flow with an authorization URL.",
    "riskImpact": "An implicit-grant token arrives in the URL fragment, so it lands in browser history, in any Referer sent by a script the page loads, and in the logs of anything that records full URLs. There is no client authentication and no PKCE, so a token intercepted at the redirect is immediately usable, and there is no refresh token, which pushes implementations toward long token lifetimes to compensate.",
    "explanation": "OAuth 2.0 Security Best Current Practice (RFC 9700) says the implicit grant must not be used, and OAuth 2.1 removes it. The replacement for browser and mobile clients is the authorization code grant with PKCE, which keeps the token out of the URL and binds the code to the client that requested it. A spec that still declares implicit usually means the authorization server still accepts it.",
    "fixSteps": [
      "Replace the implicit flow declaration with authorizationCode and require PKCE on the authorization server.",
      "Stop accepting response_type=token at the authorization endpoint once clients have migrated.",
      "Shorten the lifetime of any token that was issued through the implicit flow and rotate refresh credentials."
    ],
    "codeExamples": [
      {
        "label": "OpenAPI 3: authorization code instead of implicit",
        "language": "yaml",
        "code": "components:\n  securitySchemes:\n    oauth2:\n      type: oauth2\n      flows:\n        authorizationCode:\n          authorizationUrl: https://auth.example.com/authorize\n          tokenUrl: https://auth.example.com/token\n          scopes:\n            read: Read access\n            write: Write access"
      },
      {
        "label": "Authorization request with PKCE",
        "language": "text",
        "code": "GET /authorize\n  ?response_type=code\n  &client_id=spa-client\n  &redirect_uri=https%3A%2F%2Fapp.example.com%2Fcallback\n  &code_challenge=E9Melhoa2OwvFrEMTJguCHaoeK1t8URWbuGJSstw-cM\n  &code_challenge_method=S256\n  &state=<random>"
      }
    ],
    "references": [
      "https://datatracker.ietf.org/doc/html/rfc9700",
      "https://oauth.net/2/grant-types/implicit/"
    ],
    "cwe": "CWE-522",
    "owasp": "A07:2021",
    "_idx": 42
  },
  {
    "id": "api-asyncapi-document-exposed",
    "type": "body-pattern",
    "title": "AsyncAPI document publicly served",
    "category": "api",
    "severity": "low",
    "description": "An AsyncAPI document is served from this URL, describing the event-driven half of the system. It covers brokers, channels and topics, and the payload schema of every message that flows through them.",
    "evidence": "The response at {{url}} declares an AsyncAPI version and contains a channels section.",
    "riskImpact": "The document names the broker hostnames and ports, the exact channel and topic strings, the protocol and security scheme each server expects, and the payload schema of every message. That is the whole internal messaging topology, including queues that no public API ever touches, handed over in one file.",
    "explanation": "Unlike an OpenAPI document, which usually describes an interface that is meant to be public, an AsyncAPI document normally describes internal plumbing between services. Publishing it on the web origin is almost always accidental, typically a docs build that copied the whole spec directory into the output. Serving it is not itself a break-in, but it removes the reconnaissance step for anyone who reaches the broker another way.",
    "fixSteps": [
      "Remove the AsyncAPI document from the public build output and from anything the web server maps.",
      "Publish it to an internal developer portal or a private schema registry instead.",
      "Treat the broker addresses it exposed as known, and confirm those brokers require authentication and are not reachable from the internet."
    ],
    "codeExamples": [
      {
        "label": "Check what the build is publishing",
        "language": "bash",
        "code": "grep -rl 'asyncapi' dist public build 2>/dev/null\n\n# Exclude spec sources from the published output\necho 'asyncapi.yaml' >> .vercelignore"
      }
    ],
    "references": [
      "https://www.asyncapi.com/docs/reference/specification/latest",
      "https://owasp.org/API-Security/editions/2023/en/0xa8-security-misconfiguration/"
    ],
    "cwe": "CWE-540",
    "owasp": "A05:2021",
    "_idx": 43
  },
  {
    "id": "api-postman-collection-exposed",
    "type": "body-pattern",
    "title": "Postman collection export publicly served",
    "category": "api",
    "severity": "medium",
    "description": "A Postman collection export is served from this URL, identified by its own _postman_id field. Collections carry every request path, body and header the author saved.",
    "evidence": "The response at {{url}} is a Postman collection export.",
    "riskImpact": "A collection is a saved copy of how someone actually called the API: every path, every request body, every header. Collections routinely carry the values used while testing, so the file frequently contains a live bearer token, an API key, or a basic-auth credential in the auth block or in a saved variable, and it always contains internal routes that were never linked publicly.",
    "explanation": "These usually reach the web root by accident: a developer exports a collection into the repository to share it, and the whole repository ends up in the build output. The immediate question is not whether the file should be there, it is whether anything in it is still valid. Treat every credential and every internal hostname it contains as disclosed.",
    "fixSteps": [
      "Remove the file from the served origin and from the repository history if it carried credentials.",
      "Rotate every token, key, and password that appears anywhere in the collection or its environment file.",
      "Share collections through a Postman workspace or a private link rather than a checked-in export.",
      "Add *.postman_collection.json and *.postman_environment.json to .gitignore."
    ],
    "codeExamples": [
      {
        "label": "Find and exclude collection exports",
        "language": "bash",
        "code": "find . -name '*postman_collection.json' -o -name '*postman_environment.json'\n\ncat >> .gitignore <<'EOF'\n*.postman_collection.json\n*.postman_environment.json\nEOF"
      }
    ],
    "references": [
      "https://learning.postman.com/docs/collections/using-collections/",
      "https://cheatsheetseries.owasp.org/cheatsheets/Secrets_Management_Cheat_Sheet.html"
    ],
    "cwe": "CWE-540",
    "owasp": "A05:2021",
    "_idx": 44
  },
  {
    "id": "api-insomnia-export-exposed",
    "type": "body-pattern",
    "title": "Insomnia workspace export publicly served",
    "category": "api",
    "severity": "medium",
    "description": "An Insomnia workspace export is served from this URL, identified by the fields Insomnia writes. The export holds every saved request in the workspace, plus the environment values those requests interpolate.",
    "evidence": "The response at {{url}} is an Insomnia workspace export.",
    "riskImpact": "The export contains every saved request in the workspace: URLs, bodies, and headers, plus the environment objects those requests interpolate. Environments are where API keys and tokens are stored, so an exported workspace frequently ships working credentials alongside the exact requests they authorize.",
    "explanation": "Same root cause as a published Postman collection, different tool. Insomnia's export format nests requests and environments in one JSON document, so a single file can disclose both the internal API surface and the secrets used to call it. Assume anything in the environment section is compromised rather than trying to judge whether a particular value looks live.",
    "fixSteps": [
      "Remove the export from the served origin, and purge it from git history if it contained secrets.",
      "Rotate every credential stored in the workspace's environments.",
      "Use Insomnia's private environments (which are excluded from exports) for secrets, and share workspaces through Insomnia Sync rather than a file."
    ],
    "codeExamples": [
      {
        "label": "Find Insomnia exports in the tree",
        "language": "bash",
        "code": "grep -rl '\"__export_source\"' . --include='*.json' 2>/dev/null\n\necho 'Insomnia_*.json' >> .gitignore"
      }
    ],
    "references": [
      "https://docs.insomnia.rest/insomnia/import-export-data",
      "https://cheatsheetseries.owasp.org/cheatsheets/Secrets_Management_Cheat_Sheet.html"
    ],
    "cwe": "CWE-540",
    "owasp": "A05:2021",
    "_idx": 45
  },
  {
    "id": "api-wadl-document-exposed",
    "type": "body-pattern",
    "title": "WADL document publicly served",
    "category": "api",
    "severity": "low",
    "description": "A WADL (Web Application Description Language) document is served from this URL. Jersey, Apache CXF and other JAX-RS stacks generate one automatically and expose it unless it is explicitly disabled.",
    "evidence": "The response at {{url}} is a WADL application document.",
    "riskImpact": "The document enumerates every resource path the JAX-RS application registers, the HTTP methods each accepts, and every query, path, header and form parameter with its type. That includes internal management resources that are not linked from anywhere and were never meant to be discovered.",
    "explanation": "This is almost always unintentional rather than a decision: Jersey serves /application.wadl by default, so the surface appears without anyone adding it. Because the WADL is generated from the live resource classes, it is always accurate and always current, which makes it a better map of the application than any documentation the team wrote by hand.",
    "fixSteps": [
      "In Jersey, set jersey.config.server.wadl.disableWadl to true.",
      "In Apache CXF, remove the WADLGenerator provider from the JAX-RS server configuration.",
      "Block /application.wadl and any ?_wadl query at the reverse proxy as a second layer."
    ],
    "codeExamples": [
      {
        "label": "Jersey: disable WADL generation",
        "language": "java",
        "code": "public class AppConfig extends ResourceConfig {\n  public AppConfig() {\n    packages(\"com.example.api\");\n    property(ServerProperties.WADL_FEATURE_DISABLE, true);\n  }\n}"
      },
      {
        "label": "Nginx: block the WADL endpoints",
        "language": "nginx",
        "code": "location ~* (application\\.wadl|\\?_wadl) {\n    return 404;\n}"
      }
    ],
    "references": [
      "https://eclipse-ee4j.github.io/jersey.github.io/documentation/latest/wadl.html",
      "https://owasp.org/API-Security/editions/2023/en/0xa8-security-misconfiguration/"
    ],
    "cwe": "CWE-540",
    "owasp": "A05:2021",
    "_idx": 46
  },
  {
    "id": "api-raml-document-exposed",
    "type": "body-pattern",
    "title": "RAML API definition publicly served",
    "category": "api",
    "severity": "low",
    "description": "A RAML API definition is served from this URL, identified by the #%RAML version marker. RAML files must begin with that marker, so this matches the document itself rather than a mention of it.",
    "evidence": "The response at {{url}} begins with a #%RAML version marker.",
    "riskImpact": "A RAML file lists every resource and method the API defines, the parameters each takes, and the securedBy scheme applied to each one, including operations that are not referenced by any public client.",
    "explanation": "RAML documents normally live in a design repository or an API portal, not on the runtime origin. Finding one served from the application host usually means the whole design directory was copied into the deployment. The security value to an attacker is the same as any complete interface description: it removes the guessing from finding an endpoint whose authorization was never implemented.",
    "fixSteps": [
      "Remove the RAML file and its includes from the served origin.",
      "Publish the definition to an internal API portal or a design workspace instead.",
      "Review any resource the file exposed that you did not expect to be public, and confirm it enforces authorization."
    ],
    "codeExamples": [
      {
        "label": "Find RAML files in the deployed output",
        "language": "bash",
        "code": "grep -rl '^#%RAML' dist public build 2>/dev/null"
      }
    ],
    "references": [
      "https://github.com/raml-org/raml-spec/blob/master/versions/raml-10/raml-10.md",
      "https://owasp.org/API-Security/editions/2023/en/0xa8-security-misconfiguration/"
    ],
    "cwe": "CWE-540",
    "owasp": "A05:2021",
    "_idx": 47
  },
  {
    "id": "api-odata-metadata-document-exposed",
    "type": "body-pattern",
    "title": "OData $metadata document publicly served",
    "category": "api",
    "severity": "low",
    "description": "An OData $metadata document, describing the service's entity model, is served from this URL. OData services such as SAP Gateway, Microsoft Dynamics and ASP.NET Core OData publish one automatically.",
    "evidence": "The response at {{url}} is an OData EDMX metadata document containing entity type definitions.",
    "riskImpact": "The metadata document is a complete schema dump: every entity set, every property with its exact name and type, every navigation property linking one entity to another, and every callable function and action. Combined with OData's query syntax, which lets a caller select, filter and expand across those navigation properties, it is enough to reach data the intended UI never displays.",
    "explanation": "Publishing $metadata is part of how OData works, so this is informational rather than a defect on its own. It matters because it turns any authorization gap into an immediately exploitable one: the caller does not have to guess a property name or an entity relationship, they can read it. The real control is server-side, restricting which entity sets are exposed and which query options are permitted.",
    "fixSteps": [
      "Expose only the entity sets the API actually needs, rather than registering the whole EDM model.",
      "Restrict $expand, $select and $filter with query-option limits so navigation properties cannot be used to reach unrelated entities.",
      "Require authentication on the metadata endpoint itself if the model is not meant to be public."
    ],
    "codeExamples": [
      {
        "label": "ASP.NET Core OData: bound query options",
        "language": "csharp",
        "code": "services.AddControllers().AddOData(opt => opt\n    .AddRouteComponents(\"odata\", GetEdmModel())\n    .Select()\n    .Filter()\n    .OrderBy()\n    .SetMaxTop(100));   // no unbounded $expand, capped page size"
      },
      {
        "label": "Require auth on the metadata route",
        "language": "csharp",
        "code": "app.MapWhen(\n    ctx => ctx.Request.Path.StartsWithSegments(\"/odata/$metadata\"),\n    branch => branch.UseAuthentication().UseAuthorization());"
      }
    ],
    "references": [
      "https://docs.oasis-open.org/odata/odata/v4.01/odata-v4.01-part1-protocol.html",
      "https://owasp.org/API-Security/editions/2023/en/0xa3-broken-object-property-level-authorization/"
    ],
    "cwe": "CWE-540",
    "owasp": "A01:2021",
    "_idx": 48
  },
  {
    "id": "api-oidc-discovery-alg-none-supported",
    "type": "body-pattern",
    "title": "OIDC discovery advertises the \"none\" signing algorithm",
    "category": "api",
    "severity": "high",
    "description": "The authorization-server metadata lists \"none\" among its supported ID token signing algorithms. That means the server will issue, and by implication accept, unsigned ID tokens.",
    "evidence": "Authorization-server metadata at {{url}} lists \"none\" among its supported ID token signing algorithms.",
    "riskImpact": "An unsigned ID token is a token anyone can write. A client that follows the discovery document and honours alg \"none\" will accept a token whose sub, email and groups claims the attacker chose, which is authentication bypass with no credential involved.",
    "explanation": "OpenID Connect Core permits alg \"none\" only for the response of a token-endpoint exchange where the token never leaves the back channel, and OIDC-conformant clients are expected to reject it everywhere else. Advertising it in discovery metadata means clients that select an algorithm from that list can pick \"none\", and it strongly suggests the server accepts unsigned tokens on the paths where it verifies them. Providers that support it usually do so through a configuration flag left over from a development setup.",
    "fixSteps": [
      "Remove \"none\" from id_token_signing_alg_values_supported.",
      "Configure the issuer to sign with RS256, PS256, or ES256 only, and to reject any token whose alg header is not on that list.",
      "On the client side, pin the accepted algorithm explicitly rather than trusting the token's own alg header.",
      "Review the issuer's logs for tokens presented with alg none."
    ],
    "codeExamples": [
      {
        "label": "Client: pin the algorithm during verification",
        "language": "javascript",
        "code": "const { payload } = await jose.jwtVerify(idToken, JWKS, {\n  issuer: 'https://auth.example.com',\n  audience: 'my-client-id',\n  algorithms: ['RS256'],   // never read alg from the token itself\n});"
      },
      {
        "label": "Inspect what the issuer advertises",
        "language": "bash",
        "code": "curl -s https://auth.example.com/.well-known/openid-configuration \\\n  | jq '.id_token_signing_alg_values_supported'"
      }
    ],
    "references": [
      "https://openid.net/specs/openid-connect-discovery-1_0.html",
      "https://datatracker.ietf.org/doc/html/rfc8725"
    ],
    "cwe": "CWE-347",
    "owasp": "A07:2021",
    "_idx": 49
  },
  {
    "id": "api-oidc-discovery-implicit-flow-supported",
    "type": "body-pattern",
    "title": "OIDC discovery advertises implicit-grant response types",
    "category": "api",
    "severity": "low",
    "description": "The authorization-server metadata advertises a response type that returns a token from the authorization endpoint. The advertised value is \"token\" or \"id_token token\", neither of which comes with a code.",
    "evidence": "Authorization-server metadata at {{url}} advertises implicit-grant response types.",
    "riskImpact": "Any client that reads this document is told the implicit grant is available. A token returned that way lands in the URL fragment, so it reaches browser history, third-party scripts on the callback page, and anything that logs full URLs, and it is issued without client authentication or PKCE.",
    "explanation": "This describes what the server advertises, not what any particular client uses, so it is a Low finding rather than a confirmed weakness. It still matters: OAuth 2.0 Security Best Current Practice (RFC 9700) says the implicit grant must not be used and OAuth 2.1 removes it, so an advertised implicit response type is a migration that has not finished. Turning it off in the metadata is only cosmetic unless the authorization endpoint stops honouring it too.",
    "fixSteps": [
      "Migrate remaining clients to authorization code with PKCE.",
      "Remove implicit entries from response_types_supported once no client depends on them.",
      "Reject response_type=token and response_type=id_token token at the authorization endpoint, not only in the metadata."
    ],
    "codeExamples": [
      {
        "label": "Metadata after migration",
        "language": "json",
        "code": "{\n  \"response_types_supported\": [\"code\"],\n  \"grant_types_supported\": [\"authorization_code\", \"refresh_token\"],\n  \"code_challenge_methods_supported\": [\"S256\"]\n}"
      }
    ],
    "references": [
      "https://datatracker.ietf.org/doc/html/rfc9700",
      "https://openid.net/specs/openid-connect-discovery-1_0.html"
    ],
    "cwe": "CWE-522",
    "owasp": "A07:2021",
    "_idx": 50
  },
  {
    "id": "api-oidc-discovery-pkce-not-advertised",
    "type": "body-pattern",
    "title": "OIDC discovery omits code_challenge_methods_supported",
    "category": "api",
    "severity": "low",
    "description": "The authorization-server metadata advertises the authorization-code response type without PKCE support. code_challenge_methods_supported is the field a client reads to discover PKCE is available, and it is absent.",
    "evidence": "Authorization-server metadata at {{url}} advertises the code response type with no code_challenge_methods_supported entry.",
    "riskImpact": "A conforming client decides whether to send a code_challenge based on this field. When it is absent, well-behaved client libraries skip PKCE, which leaves the authorization code unbound to the requesting client: an attacker who intercepts the code at the redirect (a custom URI scheme another app registered, a leaked Referer, a compromised proxy) can exchange it themselves.",
    "explanation": "The metadata may be understating the server, since many implementations accept a code_challenge without advertising it. That is exactly the problem: correctness now depends on every client hardcoding PKCE rather than on discovery. RFC 8414 defines the field and RFC 9700 requires PKCE for all authorization-code clients, public and confidential alike, so publishing it is the cheap half of the fix.",
    "fixSteps": [
      "Add \"code_challenge_methods_supported\": [\"S256\"] to the metadata document.",
      "Require a code_challenge on every authorization-code request at the server, and reject the plain method.",
      "In clients, enable PKCE explicitly rather than relying on discovery to turn it on."
    ],
    "codeExamples": [
      {
        "label": "Metadata: advertise S256",
        "language": "json",
        "code": "{\n  \"issuer\": \"https://auth.example.com\",\n  \"authorization_endpoint\": \"https://auth.example.com/authorize\",\n  \"token_endpoint\": \"https://auth.example.com/token\",\n  \"response_types_supported\": [\"code\"],\n  \"code_challenge_methods_supported\": [\"S256\"]\n}"
      },
      {
        "label": "Check an issuer",
        "language": "bash",
        "code": "curl -s https://auth.example.com/.well-known/openid-configuration \\\n  | jq '{response_types_supported, code_challenge_methods_supported}'"
      }
    ],
    "references": [
      "https://datatracker.ietf.org/doc/html/rfc8414",
      "https://datatracker.ietf.org/doc/html/rfc7636"
    ],
    "cwe": "CWE-1021",
    "owasp": "A07:2021",
    "_idx": 51
  },
  {
    "id": "api-oauth-authorize-redirect-uri-insecure",
    "type": "url-check",
    "title": "OAuth authorization request uses a cleartext redirect_uri",
    "category": "api",
    "severity": "medium",
    "description": "The scanned URL is an OAuth or OpenID Connect authorization request with a cleartext redirect_uri. The callback is a plain http URL on a host that is not loopback.",
    "evidence": "Authorization request at {{url}} carries an http:// redirect_uri on a non-loopback host.",
    "riskImpact": "The authorization response, carrying either the authorization code or, in an implicit flow, the token itself, is delivered to that cleartext URL. Anyone on the network path reads it, and with the code in hand can complete the exchange before the real client does unless PKCE is enforced.",
    "explanation": "RFC 9700 requires redirect URIs to use https, with one exception: loopback addresses for native applications, which RFC 8252 section 7.3 explicitly permits over http. This check honours that exception and does not fire on localhost, 127.0.0.1 or ::1. Everything else is a real downgrade, whether the redirect target is first-party or not.",
    "fixSteps": [
      "Change the client's registered redirect URI to https.",
      "Configure the authorization server to reject any non-loopback http redirect_uri at registration time, not only at request time.",
      "Treat any code or token already delivered over http as disclosed and revoke the associated grants."
    ],
    "codeExamples": [
      {
        "label": "Correct authorization request",
        "language": "text",
        "code": "GET /authorize\n  ?response_type=code\n  &client_id=web-app\n  &redirect_uri=https%3A%2F%2Fapp.example.com%2Fcallback\n  &code_challenge_method=S256\n  &code_challenge=<challenge>\n  &state=<random>"
      },
      {
        "label": "Server: reject cleartext redirect URIs",
        "language": "javascript",
        "code": "function isAllowedRedirect(uri) {\n  const u = new URL(uri);\n  if (u.protocol === 'https:') return true;\n  // RFC 8252 7.3: loopback over http is permitted for native apps\n  return u.protocol === 'http:' &&\n    ['localhost', '127.0.0.1', '::1'].includes(u.hostname);\n}"
      }
    ],
    "references": [
      "https://datatracker.ietf.org/doc/html/rfc9700",
      "https://datatracker.ietf.org/doc/html/rfc8252#section-7.3"
    ],
    "cwe": "CWE-319",
    "owasp": "A02:2021",
    "_idx": 52
  },
  {
    "id": "api-oauth-authorize-oidc-nonce-missing",
    "type": "url-check",
    "title": "OIDC authorization request returns an ID token with no nonce",
    "category": "api",
    "severity": "medium",
    "description": "The scanned URL is an authorization request whose response_type includes id_token, but it carries no nonce parameter. OpenID Connect Core section 3.2.2.1 requires a nonce for every flow that returns an ID token from the authorization endpoint.",
    "evidence": "Authorization request at {{url}} asks for an id_token response type and includes no nonce parameter.",
    "riskImpact": "The nonce is what binds an ID token to the specific authentication request that asked for it. Without it, an ID token captured from one session can be replayed into another: an attacker who obtains a valid token for a victim, from browser history, a logged URL, or a different relying party, can present it at the callback and be logged in as that user.",
    "explanation": "state and nonce solve different problems and neither substitutes for the other. state protects the redirect against CSRF; nonce protects the token against replay, and it is the only one carried inside the token itself so the client can check it after the fact. The value must be unguessable, stored with the session that started the flow, and compared against the token's nonce claim on return.",
    "fixSteps": [
      "Generate a cryptographically random nonce per authorization request and include it in the request.",
      "Persist it against the user's session, not in a cookie the callback page can read from JavaScript.",
      "On callback, reject the ID token unless its nonce claim equals the stored value, then discard the stored value so it cannot be reused.",
      "Prefer the authorization code flow with PKCE, where the token is never exposed in the front channel at all."
    ],
    "codeExamples": [
      {
        "label": "Generate and store the nonce",
        "language": "javascript",
        "code": "const nonce = crypto.randomUUID();\nreq.session.oidcNonce = nonce;\n\nconst authUrl = new URL('https://auth.example.com/authorize');\nauthUrl.searchParams.set('response_type', 'code id_token');\nauthUrl.searchParams.set('client_id', CLIENT_ID);\nauthUrl.searchParams.set('redirect_uri', CALLBACK);\nauthUrl.searchParams.set('scope', 'openid profile');\nauthUrl.searchParams.set('nonce', nonce);\nauthUrl.searchParams.set('state', crypto.randomUUID());"
      },
      {
        "label": "Verify it on callback",
        "language": "javascript",
        "code": "const { payload } = await jose.jwtVerify(idToken, JWKS, {\n  issuer: ISSUER,\n  audience: CLIENT_ID,\n  algorithms: ['RS256'],\n});\nif (payload.nonce !== req.session.oidcNonce) {\n  throw new Error('nonce mismatch');\n}\ndelete req.session.oidcNonce;   // single use"
      }
    ],
    "references": [
      "https://openid.net/specs/openid-connect-core-1_0.html#AuthRequest",
      "https://datatracker.ietf.org/doc/html/rfc9700"
    ],
    "cwe": "CWE-294",
    "owasp": "A07:2021",
    "_idx": 53
  },
  {
    "id": "api-jwt-long-lived-token",
    "type": "body-pattern",
    "title": "JWT with a lifetime measured in months",
    "category": "api",
    "severity": "low",
    "description": "A JWT reachable from this response stays valid for more than 90 days after it was issued. The decoded payload's exp claim is more than 90 days after its iat or nbf.",
    "evidence": "A JWT found at {{url}} declares a lifetime of more than 90 days between its issue and expiry claims.",
    "riskImpact": "Expiry is the only revocation a stateless JWT has. A token that lives for months means a copy taken from a log, a browser profile, a crash report, or an old backup keeps working long after the account it belongs to has changed password, lost a role, or been deleted, unless a separate denylist is consulted on every request.",
    "explanation": "The claims are read by base64url-decoding the payload; no signature verification and no network request is involved, so this says nothing about whether the token is currently valid. Long-lived JWTs are sometimes deliberate, refresh tokens and machine-to-machine credentials are the usual cases, which is why this is Low rather than higher. What it is worth checking is that the long-lived token is a refresh credential held server-side, not an access token being sent to a browser.",
    "fixSteps": [
      "Keep access-token lifetimes in the minutes-to-an-hour range and use a refresh token for continuity.",
      "Give refresh tokens a jti and check it against a server-side store on use, so revocation is possible.",
      "Rotate refresh tokens on each use and invalidate the previous one, so a stolen copy stops working after the legitimate client next refreshes."
    ],
    "codeExamples": [
      {
        "label": "Short access token, revocable refresh token",
        "language": "javascript",
        "code": "const accessToken = await new jose.SignJWT({ sub: user.id, scope })\n  .setProtectedHeader({ alg: 'RS256' })\n  .setIssuedAt()\n  .setExpirationTime('15m')\n  .sign(privateKey);\n\nconst refreshToken = await new jose.SignJWT({ sub: user.id, jti })\n  .setProtectedHeader({ alg: 'RS256' })\n  .setIssuedAt()\n  .setExpirationTime('30d')\n  .sign(privateKey);\n\nawait db.storeRefreshJti(jti, user.id);   // makes revocation possible"
      },
      {
        "label": "Inspect a token's lifetime",
        "language": "bash",
        "code": "echo \"$JWT\" | cut -d. -f2 | base64 -d 2>/dev/null | jq '{iat, exp, days: ((.exp - .iat) / 86400)}'"
      }
    ],
    "references": [
      "https://datatracker.ietf.org/doc/html/rfc8725#section-3.10",
      "https://cheatsheetseries.owasp.org/cheatsheets/JSON_Web_Token_for_Java_Cheat_Sheet.html"
    ],
    "cwe": "CWE-613",
    "owasp": "A07:2021",
    "_idx": 54
  },
  {
    "id": "api-retry-after-invalid-value",
    "type": "header-value",
    "title": "Retry-After header is not a valid delay or date",
    "category": "api",
    "severity": "low",
    "description": "The response carries a Retry-After header whose value is neither seconds nor an HTTP-date. Those are the only two forms RFC 9110 defines, and a client that cannot parse the header ignores it.",
    "evidence": "Retry-After on the response to {{url}} is not a parseable value.",
    "riskImpact": "Clients that cannot parse the header fall back to their own retry policy, which is usually an immediate retry or a fixed short interval. Under the load that produced the 429 or 503 in the first place, that turns a throttled client into a retry storm, and it defeats the backoff the header exists to coordinate.",
    "explanation": "Common wrong values are \"60s\", an ISO-8601 timestamp, a float, or a bare word. The two legal forms are a decimal count of seconds (\"120\") and an IMF-fixdate (\"Wed, 21 Oct 2026 07:28:00 GMT\"). This check reads the header only; it does not judge whether the delay itself is reasonable.",
    "fixSteps": [
      "Emit either an integer number of seconds or an IMF-fixdate, with no unit suffix.",
      "Set it on every 429 and on 503 responses that come from a maintenance or overload path.",
      "Test the value with a client library rather than by eye: most will silently ignore what they cannot parse."
    ],
    "codeExamples": [
      {
        "label": "Express: valid Retry-After forms",
        "language": "javascript",
        "code": "// Seconds\nres.set('Retry-After', '120');\n\n// Or an absolute time\nres.set('Retry-After', new Date(Date.now() + 120_000).toUTCString());\n\nres.status(429).json({ error: 'rate_limited' });"
      },
      {
        "label": "Nginx: fixed backoff on throttle",
        "language": "nginx",
        "code": "limit_req_status 429;\n\nerror_page 429 = @throttled;\nlocation @throttled {\n    add_header Retry-After 60 always;\n    return 429;\n}"
      }
    ],
    "references": [
      "https://httpwg.org/specs/rfc9110.html#field.retry-after",
      "https://datatracker.ietf.org/doc/html/rfc6585"
    ],
    "cwe": "CWE-20",
    "owasp": "A05:2021",
    "_idx": 55
  },
  {
    "id": "api-sunset-header-in-past",
    "type": "header-value",
    "title": "Sunset date has passed but the endpoint still answers",
    "category": "api",
    "severity": "low",
    "description": "The response carries an RFC 8594 Sunset header whose date has already passed. The resource is still being served after the retirement date it announced.",
    "evidence": "The response to {{url}} carries a Sunset date that has already passed.",
    "riskImpact": "A route that outlives its own sunset date is a route nobody owns. It keeps running the code path that was frozen at deprecation, including its authentication and input validation, while review, testing and dependency updates have moved to the replacement. It is also the version an attacker will pick, precisely because it is the one that stopped changing.",
    "explanation": "Sunset is a promise about a date, and the header still being served past that date means either the removal slipped or the header was never revisited. Both are worth knowing. The check only reads the header; it does not judge whether keeping the endpoint alive was the right call, and a deliberate extension should be reflected by updating the date rather than by leaving a stale one in place.",
    "fixSteps": [
      "Decide whether to remove the endpoint or extend it, then make the header say so.",
      "If extending, publish a new Sunset date and tell the callers that are still using it.",
      "If removing, return 410 Gone with a Link rel=successor-version pointing at the replacement, rather than deleting the route silently.",
      "Add a check to CI or to a monitor that alerts when a Sunset date passes while the route is still live."
    ],
    "codeExamples": [
      {
        "label": "Retire the route properly",
        "language": "javascript",
        "code": "router.all('/v1/reports', (req, res) => {\n  res.set('Link', '</v2/reports>; rel=\"successor-version\"');\n  res.status(410).json({\n    error: 'gone',\n    message: 'v1/reports was retired on 2026-01-01. Use /v2/reports.',\n  });\n});"
      },
      {
        "label": "Check a live endpoint",
        "language": "bash",
        "code": "curl -sI https://api.example.com/v1/reports | grep -i -E 'sunset|deprecation|link'"
      }
    ],
    "references": [
      "https://datatracker.ietf.org/doc/html/rfc8594",
      "https://datatracker.ietf.org/doc/html/rfc9745"
    ],
    "cwe": "CWE-1059",
    "owasp": "A09:2021",
    "_idx": 56
  },
  {
    "id": "api-json-response-content-type-mismatch",
    "type": "combined",
    "title": "JSON body served as HTML or with no Content-Type",
    "category": "api",
    "severity": "low",
    "description": "The response body is JSON-shaped but is not labelled as JSON by its Content-Type header. It is either declared as text/html, or served with no Content-Type at all.",
    "evidence": "The response at {{url}} has a JSON-shaped body with an HTML or absent Content-Type.",
    "riskImpact": "A JSON document labelled text/html is parsed as markup. Any string value in it that an attacker controls, a username, a search term, an error message echoing input, becomes live HTML in the browser, which is stored or reflected XSS on your own origin. With no Content-Type at all the browser sniffs, and a body whose first bytes look like markup is treated as a document.",
    "explanation": "The detector checks structure rather than parsing the whole body: it requires the response to open with a JSON object or array and to have no HTML document markers near the start, so an HTML page that happens to embed JSON does not match. X-Content-Type-Options: nosniff blocks the sniffing half of this but does not help when the server explicitly says text/html, so the Content-Type itself has to be correct.",
    "fixSteps": [
      "Return Content-Type: application/json (or application/problem+json for errors) on every JSON response, including error paths.",
      "Check the framework's error handler specifically: it is the usual place a JSON API falls back to the default HTML content type.",
      "Send X-Content-Type-Options: nosniff as a second layer so a missing type never gets sniffed into a document."
    ],
    "codeExamples": [
      {
        "label": "Express: JSON content type on errors too",
        "language": "javascript",
        "code": "app.use((err, req, res, _next) => {\n  res.status(err.status || 500)\n     .type('application/problem+json')\n     .json({ title: 'Request failed', status: err.status || 500 });\n});"
      },
      {
        "label": "Next.js Route Handler",
        "language": "typescript",
        "code": "return new Response(JSON.stringify(payload), {\n  status: 200,\n  headers: {\n    'Content-Type': 'application/json; charset=utf-8',\n    'X-Content-Type-Options': 'nosniff',\n  },\n});"
      }
    ],
    "references": [
      "https://cheatsheetseries.owasp.org/cheatsheets/Cross_Site_Scripting_Prevention_Cheat_Sheet.html",
      "https://developer.mozilla.org/en-US/docs/Web/HTTP/Headers/X-Content-Type-Options"
    ],
    "cwe": "CWE-436",
    "owasp": "A03:2021",
    "_idx": 57
  },
  {
    "id": "api-response-header-internal-host",
    "type": "header-value",
    "title": "Response header points at an internal host",
    "category": "api",
    "severity": "low",
    "description": "A Location, Content-Location or Link response header contains a URL on an internal hostname. That means a loopback or RFC1918 address, or a private-use suffix such as .internal, .local or .corp.",
    "evidence": "A response header for {{url}} carries a URL on an internal hostname.",
    "riskImpact": "The header names a host that exists behind the proxy: an internal service name, a pod address, an admin origin. That is free reconnaissance for anyone who later reaches the network another way, and where the URL is a pagination Link, it also tells a client to fetch an address it cannot resolve, which turns into a hard failure rather than a graceful one.",
    "explanation": "This is nearly always a backend building absolute URLs from its own request context rather than from the public origin, so the internal hostname survives the trip through the proxy. The fix is to construct outward-facing URLs from a configured public base, not from the incoming Host header or the container's own name. Relative URLs sidestep the problem entirely where the header allows them.",
    "fixSteps": [
      "Build absolute URLs in responses from an explicit PUBLIC_BASE_URL setting, not from req.headers.host or the local hostname.",
      "Prefer relative references in Location and Link where the specification allows them.",
      "Add a response filter at the edge that rejects or rewrites headers containing private hostnames, as a backstop."
    ],
    "codeExamples": [
      {
        "label": "Build links from a configured public origin",
        "language": "javascript",
        "code": "const PUBLIC_BASE = new URL(process.env.PUBLIC_BASE_URL);\n\nfunction pageLink(page) {\n  const u = new URL('/v1/items', PUBLIC_BASE);\n  u.searchParams.set('page', String(page));\n  return u.toString();\n}\n\nres.set('Link', `<${pageLink(next)}>; rel=\"next\"`);"
      },
      {
        "label": "Nginx: keep redirects on the public origin",
        "language": "nginx",
        "code": "proxy_redirect http://app-internal:3000/ https://api.example.com/;\nabsolute_redirect off;"
      }
    ],
    "references": [
      "https://cwe.mitre.org/data/definitions/200.html",
      "https://owasp.org/API-Security/editions/2023/en/0xa8-security-misconfiguration/"
    ],
    "cwe": "CWE-200",
    "owasp": "A05:2021",
    "_idx": 58
  },
  {
    "id": "api-www-authenticate-realm-internal-detail",
    "type": "header-value",
    "title": "WWW-Authenticate realm leaks internal detail",
    "category": "api",
    "severity": "low",
    "description": "The WWW-Authenticate challenge carries a realm string that describes internal infrastructure. The value contains a filesystem path, an IP address, or an internal hostname suffix.",
    "evidence": "The authentication challenge for {{url}} exposes internal detail in its realm value.",
    "riskImpact": "The realm is shown to anyone who requests the resource, including in the browser's own credential prompt. A realm like \"/var/www/internal-admin\" or \"backup-01.corp\" hands over the server's directory layout or an internal hostname before any credential is supplied, and it tells an attacker which part of the estate they have reached.",
    "explanation": "Realms usually pick up these values because the web server defaults to the protected directory path, or because someone used the machine name as a label. The realm has exactly one functional job: it lets a client group credentials that work in the same protection space. A short, opaque, stable label does that job without describing the infrastructure.",
    "fixSteps": [
      "Replace the realm with a short opaque label that names the service, not the path or the host.",
      "Keep the value stable, since clients cache credentials against it.",
      "Check the web server's default: Apache and Nginx templates often interpolate the directory being protected."
    ],
    "codeExamples": [
      {
        "label": "Nginx: opaque realm",
        "language": "nginx",
        "code": "location /admin/ {\n    auth_basic \"restricted\";\n    auth_basic_user_file /etc/nginx/.htpasswd;\n}"
      },
      {
        "label": "Express: opaque realm on the challenge",
        "language": "javascript",
        "code": "res.set('WWW-Authenticate', 'Bearer realm=\"api\", error=\"invalid_token\"');\nres.status(401).end();"
      }
    ],
    "references": [
      "https://httpwg.org/specs/rfc9110.html#field.www-authenticate",
      "https://cwe.mitre.org/data/definitions/200.html"
    ],
    "cwe": "CWE-200",
    "owasp": "A05:2021",
    "_idx": 59
  },
  {
    "id": "api-problem-json-trace-exposed",
    "type": "body-pattern",
    "title": "Problem document exposes a stack trace or exception class",
    "category": "api",
    "severity": "medium",
    "description": "An RFC 9457 problem document exposes a stack trace or the internal exception class behind it. The trace member carries real stack frames, or the exception member names the class that threw.",
    "evidence": "The problem document returned for {{url}} contains stack frames or an internal exception class name.",
    "riskImpact": "A stack trace gives away the framework and its version, the absolute paths of deployed files, the internal package and class layout, and often the exact line where input handling failed. That is the map an attacker uses to pick which known vulnerability applies and where to aim the next request.",
    "explanation": "Spring Boot produces this shape when server.error.include-stacktrace is set to always, which is the default in some starter templates and is frequently left on after debugging. The problem-document format itself is fine and worth using; what must not travel with it are the trace and exception members. A stable error code plus a correlation id gives support everything it needs while keeping the internals server-side.",
    "fixSteps": [
      "Set server.error.include-stacktrace=never and server.error.include-exception=false in production (or the equivalent in your framework).",
      "Return a stable machine-readable error code and a correlation id in the problem document instead of the trace.",
      "Log the full exception server-side against that same correlation id.",
      "Test the error path in the production profile, not only in development, since the two usually differ."
    ],
    "codeExamples": [
      {
        "label": "Spring Boot: keep traces server-side",
        "language": "properties",
        "code": "server.error.include-stacktrace=never\nserver.error.include-exception=false\nserver.error.include-message=never\nserver.error.include-binding-errors=never"
      },
      {
        "label": "A safe problem document",
        "language": "json",
        "code": "{\n  \"type\": \"https://api.example.com/errors/invalid-parameter\",\n  \"title\": \"Invalid parameter\",\n  \"status\": 400,\n  \"detail\": \"startDate must be before endDate\",\n  \"instance\": \"/v1/reports\",\n  \"correlationId\": \"0f2c1b7e-9a1d-4a44-8f2e-6b6d0f9a1c33\"\n}"
      }
    ],
    "references": [
      "https://datatracker.ietf.org/doc/html/rfc9457",
      "https://cheatsheetseries.owasp.org/cheatsheets/Error_Handling_Cheat_Sheet.html"
    ],
    "cwe": "CWE-209",
    "owasp": "A05:2021",
    "_idx": 60
  },
  {
    "id": "api-swagger-ui-outdated-version",
    "type": "body-pattern",
    "title": "Outdated Swagger UI bundle loaded",
    "category": "api",
    "severity": "medium",
    "description": "The page loads a Swagger UI bundle older than 4.1.3, read from the version embedded in the asset path or package specifier.",
    "evidence": "A Swagger UI bundle earlier than 4.1.3 is referenced from {{url}}.",
    "riskImpact": "Swagger UI before 4.1.3 is affected by a DOM XSS in its own rendering of a specification (CVE-2021-46708). Because Swagger UI is commonly configured to load a spec from a URL supplied in the query string, an attacker can serve a crafted specification and get script execution on the origin hosting the documentation, which is often the same origin as the API session cookie.",
    "explanation": "The version is read from the asset path or the package specifier, so it reports what the page references rather than what a CDN might silently substitute. Documentation pages are easy to forget in dependency upgrades because they are usually a static bundle pinned once and never touched again. If the page also accepts a ?url= or ?configUrl= parameter, the exposure is direct rather than theoretical.",
    "fixSteps": [
      "Upgrade swagger-ui-dist to a current release and redeploy the documentation page.",
      "Do not let the spec URL come from the query string; hardcode it, or validate it against an allowlist.",
      "Serve the documentation from a separate origin from the authenticated application so an XSS there cannot reach session cookies.",
      "Pin the bundle with a Subresource Integrity hash if it is loaded from a CDN."
    ],
    "codeExamples": [
      {
        "label": "Hardcode the spec URL",
        "language": "javascript",
        "code": "SwaggerUIBundle({\n  url: '/openapi.json',          // never new URLSearchParams(...).get('url')\n  dom_id: '#swagger-ui',\n  deepLinking: false,\n});"
      },
      {
        "label": "Pin the bundle with SRI",
        "language": "html",
        "code": "<script\n  src=\"https://cdn.jsdelivr.net/npm/swagger-ui-dist@5.17.14/swagger-ui-bundle.js\"\n  integrity=\"sha384-REPLACE_WITH_REAL_HASH\"\n  crossorigin=\"anonymous\"></script>"
      }
    ],
    "references": [
      "https://github.com/swagger-api/swagger-ui/releases",
      "https://nvd.nist.gov/vuln/detail/CVE-2021-46708"
    ],
    "cwe": "CWE-79",
    "owasp": "A06:2021",
    "_idx": 61
  },
  {
    "id": "api-cors-allow-origin-multiple-values",
    "type": "header-value",
    "title": "Access-Control-Allow-Origin carries more than one origin",
    "category": "api",
    "severity": "low",
    "description": "The Access-Control-Allow-Origin header holds two or more origins rather than one. The header is defined to carry exactly one origin, or the single token *.",
    "evidence": "Access-Control-Allow-Origin on the response to {{url}} contains multiple origin values.",
    "riskImpact": "No browser accepts a multi-valued Access-Control-Allow-Origin, so every cross-origin call to this endpoint fails the CORS check regardless of which origin made it. The usual repair, once someone notices the breakage, is to replace the list with a wildcard or with unconditional reflection of the request's Origin, and both of those are materially worse than the list that was there.",
    "explanation": "The header is written this way when a server joins its allowlist into one string instead of matching the incoming Origin against the list and echoing only the match. Serving the whole list also discloses which partner origins are trusted. The correct shape is one origin per response, chosen by comparing the request's Origin against the allowlist, with Vary: Origin so caches keep the answers apart.",
    "fixSteps": [
      "Match the request's Origin against the allowlist and echo only that single value.",
      "Send no Access-Control-Allow-Origin at all when the Origin is not on the list, rather than sending the list.",
      "Add Vary: Origin so a shared cache cannot serve one origin's response to another."
    ],
    "codeExamples": [
      {
        "label": "Echo one matched origin",
        "language": "javascript",
        "code": "const ALLOWED = new Set([\n  'https://app.example.com',\n  'https://admin.example.com',\n]);\n\napp.use((req, res, next) => {\n  const origin = req.headers.origin;\n  if (origin && ALLOWED.has(origin)) {\n    res.set('Access-Control-Allow-Origin', origin);\n    res.set('Access-Control-Allow-Credentials', 'true');\n  }\n  res.append('Vary', 'Origin');\n  next();\n});"
      },
      {
        "label": "Verify per origin",
        "language": "bash",
        "code": "for o in https://app.example.com https://evil.example; do\n  echo \"$o\"\n  curl -sI -H \"Origin: $o\" https://api.example.com/v1/items \\\n    | grep -i access-control-allow-origin\ndone"
      }
    ],
    "references": [
      "https://fetch.spec.whatwg.org/#http-access-control-allow-origin",
      "https://developer.mozilla.org/en-US/docs/Web/HTTP/Headers/Access-Control-Allow-Origin"
    ],
    "cwe": "CWE-346",
    "owasp": "A05:2021",
    "_idx": 62
  },
  {
    "id": "api-cors-credentials-without-allow-origin",
    "type": "header-value",
    "title": "Access-Control-Allow-Credentials sent with no allowed origin",
    "category": "api",
    "severity": "info",
    "description": "The response sets Access-Control-Allow-Credentials: true with no Access-Control-Allow-Origin. There is no origin the credentialed cross-origin request could be allowed for.",
    "evidence": "The response to {{url}} sets Access-Control-Allow-Credentials: true with no Access-Control-Allow-Origin.",
    "riskImpact": "No browser will honour the credentialed cross-origin request, so this configuration cannot work as intended. That matters less for what it permits than for what it signals: a CORS policy that is half-applied, typically because the Allow-Origin half is set conditionally on a code path that did not run, while the credentials flag is set unconditionally.",
    "explanation": "Reported as informational because in this state the header grants nothing. It is worth fixing because the two halves are meant to be decided together: whoever adds the missing Allow-Origin is one step from reflecting the request's Origin unconditionally, which combined with the credentials flag already present here is the arbitrary-origin-with-credentials case that is genuinely critical. Set both from the same allowlist check, or neither.",
    "fixSteps": [
      "Set Access-Control-Allow-Origin and Access-Control-Allow-Credentials in the same branch, after the origin has been matched against an allowlist.",
      "Never pair the credentials flag with a reflected or wildcard origin.",
      "Send Vary: Origin whenever the response depends on the request's Origin.",
      "Drop the credentials flag entirely if the endpoint does not need cookies or HTTP authentication."
    ],
    "codeExamples": [
      {
        "label": "Set both together or neither",
        "language": "javascript",
        "code": "const ALLOWED = new Set(['https://app.example.com']);\n\napp.use((req, res, next) => {\n  const origin = req.headers.origin;\n  if (origin && ALLOWED.has(origin)) {\n    res.set('Access-Control-Allow-Origin', origin);\n    res.set('Access-Control-Allow-Credentials', 'true');\n  }\n  res.append('Vary', 'Origin');\n  next();\n});"
      }
    ],
    "references": [
      "https://fetch.spec.whatwg.org/#http-access-control-allow-credentials",
      "https://portswigger.net/web-security/cors"
    ],
    "cwe": "CWE-346",
    "owasp": "A05:2021",
    "_idx": 63
  }
]
