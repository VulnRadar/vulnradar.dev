# Build output
.next/
out/
build/
dist/
coverage/
extension/dist-chrome/
extension/dist-firefox/
extension/dist-build/
node_modules/

# Lock files
package-lock.json
extension/package-lock.json

# Generated by scripts/knowledge/compile-*-knowledge.mjs on every predev and prebuild.
# Formatting these makes `npm run format:check` fail the moment anyone runs a
# build, because the generator writes its own layout back over Prettier's.
lib/ai/checks-knowledge.md
lib/ai/checks-index.md
lib/ai/docs-knowledge.md
lib/ai/changelog-knowledge.md
lib/ai/legal-knowledge.md
lib/ai/features-knowledge.md
# The retrieval index, from compile-knowledge-index.mjs. Same reason as the
# .md files above, and one more: it is written minified on purpose. Prettier
# expands it from ~250 KB to ~450 KB of a file the server parses on boot and
# no human reads.
lib/ai/knowledge-index.json
